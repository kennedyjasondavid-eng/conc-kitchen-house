# Slice E-1 — EXPO shell / boot / storage / matching / decomposition (L1–12,074)

Read `_COMMON.md` first. Findings → `findings/E-1.md`. Repo: `/home/user/conc-kitchen-expo` (record HEAD sha).

**App:** `index.html`, 40,463 lines — the production scheduler, most complex app in HOUSE. `APP_VERSION` ~v9.49.36.

**Attentive range: L1–12,074** — CSS + HTML shell, boot sequence, the storage layer, head data tables (`SITES` @~2074, `_DATA_SOURCES_DEFAULTS` @~2152, `SITE_PROFILE_SRC` @~2444, `EMBEDDED_SITE_PROFILE`, `scheduleMode` decl ~@2414 post-TDZ-fix), equipment classification (~@10494), recipe matching (~@10580; `NAME_ALIASES` @~9272, ~284 entries), menu decomposition (~@10769).

**You also own, whole-file (grep/script scale, not attentive):**
1. The full grep battery from _COMMON.md over all 40,463 lines (E-2/E-3 will read their ranges attentively; you own the whole-file counts).
2. Inline-handler set-diff: every `on\w+="fn(...)"` name in the HTML vs defined functions — name any handler calling a nonexistent function (silently-dead class).
3. **The boot-timeline artifact** (required output section): enumerate in source order every parse-time IIFE / top-level side-effecting statement and every boot-time async op (fetches, storage reads, migrations), each annotated with what state it ASSUMES initialized. Flag: (a) parse-time readers of later `let`/`const` — the TDZ class; one such crash shipped (v9.49.28, fixed by moving `scheduleMode` up); hunt SIBLINGS; (b) async pairs where B consumes A's write with no ordering guarantee; (c) `typeof x === 'function'` guards that check the function exists but not the bindings it reads (documented false-safeguard siblings ~:27262 / ~:29329 — verify those and hunt more).

**Leads to deepen:** EXPO has 132 empty catches (51% of all its catches) — triage the ones on boot/storage paths specifically. `pp_*` key handling + `conc-schedule-mode`. The baked `MENU` fallback being stale vs DOOR is KNOWN (EXPO #233 in flight) — do NOT re-report the staleness; DO report structural weaknesses in the resync *mechanism* itself if you find them. `NAME_ALIASES`: script-check for dead aliases (targets absent from `RECIPE_DB` keys) and duplicate keys.

**Skim-class:** the mega single-line data tables (RECIPE_DB etc.) — consistency checks by script only; CSS — sampled dead-selector check only.
