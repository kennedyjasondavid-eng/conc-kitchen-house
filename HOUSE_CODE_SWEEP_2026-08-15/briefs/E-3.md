# Slice E-3 — EXPO tail: panels, sync, settings (L26,910–40,463) + test-suite health

Read `_COMMON.md` first. Findings → `findings/E-3.md`. Repo: `/home/user/conc-kitchen-expo` (record HEAD sha).

**Attentive range: L26,910–40,463** — pins/panels/My-Changes tail, settings + data sources, DOOR menu sync (`syncMenuFromDOOR` ~@35682 previewer vs `loadMenuFromDOOR` fetch-and-apply — the documented footgun #1 pair; verify current honesty), boot fetch fan-in (@35272–36391: codex/door/registry/routing), unresolved-dish / New Dishes surfaces, `build_info.json` reader (@~40375; the committed file has `version:""` — root-cause: is the env badge silently degraded? silently-dead class), snapshot drain + storage-budget machinery in range.

**You also own: the EXPO test-suite health lane** (grep/inventory scale):
1. `.github/workflows/schedule-gate.yml` `verb-gates` job — extract its ENUMERATED gate list; diff vs `tests/*_gate.mjs` on disk. Name every gate that exists but never runs in CI (`jerk_tofu_menu_parity_gate` is KNOWN — find the rest), and any enumerated-but-missing entries. This is the "enforcement blind spot" lens (a green-locally-only gate enforces nothing).
2. The documented cumulative-load flake set (`placement_reader`, `production_path_edit`, `step_value_set`, `nonmenu_add`) — is it tracked anywhere machine-readable, or only in prose? (A flake list living only in CLAUDE.md is a doc-drift risk.)
3. `snapshots/` staleness by script: 44 snapshot files — date distribution, `latest.json` age, total repo weight (~40MB?); is anything still consuming them (README claims vs reality)?
4. `tests/ux_baseline.json` row count (recon says 532; docs say 533) — count and note.
5. Read the gate-runner/config only — NOT the 153 test bodies.

**Doc-drift spot-checks to include:** CLAUDE.md says "~34K lines" and v9.49.35 — actual is 40,463 and v9.49.36+; batch the stale-doc class as ONE finding with the best 3–5 examples (front-door prompts pointing at closed lines, etc.).

**Boundary note:** publish atomicity = X-B. Storage-key cross-app collisions = X-C. Menu-contract field conformance = X-A. Stay app-side.
