# Slice P-1 — PROOF full + HOUSE portal + two house-wide lanes (CI map · TZ/dates)

Read `_COMMON.md` first. Findings → `findings/P-1.md`. Repos: `/home/user/conc-kitchen-proof` + `/home/user/conc-kitchen-house` (record both HEAD shas).

## Scope A — PROOF (`proof.html`, 2,603 lines, attentive in full)

Funder/board reporting. Pure compute core L331–1269 (DOM-free; tests vm-eval it) + IO/render L1271–2601. Recently audited (Jul 17 seam audit) — marginal yield is in the corners, not the center.

Verify/deepen these specific leads at exact lines:
1. **Fixture discrepancy (resolve firsthand):** `tests/fixtures/tier1/hub_schedule.json` was reported both as 0 bytes and as 325KB by two recon passes — `ls -la` the fixtures tree; there may be two paths. Then: fixtures generation-stale (routing v30 vs live v31; hub_schedule pre-`_schemaVersion`; recipe_production generated 2026-05-08 vs live 2026-07-28) — what is the test suite NOT testing because fixtures predate current schemas?
2. **Embedded `SAMPLE` fallback cycle anchor `'2026-05-10'`** (@~1308, @~1559) vs the live anchor 2026-06-07: trace exactly when SAMPLE renders (offline first-boot only? cache-miss?), and what a wrong anchor would show a funder/board member (wrong week labeling? wrong proration?). Confidence needs the full trace.
3. `proof.html` @~972 cites "INSIGHTS #165/#167" but PROOF's INSIGHTS.md has 21 insights — verify, and check for sibling stale citations.
4. `SUBSTRATE.md` internal disagreement (menu v31 in §3 table vs v32 in its own amendment).
5. `recipe_aliases.DRAFT.json` (v0, "staged, not yet published") committed in PROOF while MISE publishes v2 — second-copy/staleness lens: can the DRAFT ever be loaded by mistake? Who's meant to delete it?
6. **Tier-2 "lens never vault" invariant:** trace that no Tier-2 (LEDGER/CREW/LOG) value can reach `cacheSet`/`prefSet`/any localStorage write. `PREF_WHITELIST` enforcement edges.
7. Validators "loud, never throw" — verify a malformed feed can't take down the whole render (partial-data path).

## Scope B — HOUSE portal (`conc-kitchen-house/index.html`, 506 lines, attentive)

- **The EXPO-ACCESS PIN dialog** (`#pin-title`, script @~353): what does it protect, is the PIN literal in source (public repo — if so it's obfuscation-not-security; grade honestly: is that *fine* for its threat model, or does it guard something that matters?), and does any real gate exist behind it?
- Link rot: every app link vs the live Pages URLs; any stale references.

## Lane 1 — house-wide CI/gate map (all six repos; inventory scale)

Per repo: tests-on-disk vs tests-run-in-CI. DOOR `door-tests.yml` runs `node --test tests/*.mjs` (all — verify); EXPO `schedule-gate.yml` `verb-gates` runs an ENUMERATED list — E-3 diffs that (coordinate: you produce the six-repo summary table; cite E-3 for EXPO detail if its findings file exists, else do the diff yourself); HUB 4 gates vs its tests dir; MISE `all.mjs` globs all; **PROOF: 12 gates incl. a 1,251-line Playwright render gate, HOUSE: portal — both have ZERO `.github/workflows`**. Quantify what is unenforced-on-push per repo (the "enforcement blind spot" lens) + the asymmetry as a house-level finding.

## Lane 2 — house-wide TZ/date-math (all six apps; targeted)

Grep all apps for `new Date(` with string args + date parsing/formatting sites. Classify each LOCAL vs UTC: `new Date("YYYY-MM-DD")` parses as UTC midnight → in Toronto (UTC-4/-5) that's the *previous* local day. Check specifically: HUB `projectDates`/`getTodayInfo` local-ISO claims; EXPO `HUB_CYCLE_START = new Date(2026,5,7)` (local) vs `_cycleStart:"2026-06-07"` string consumed by HUB/PROOF parsers — mixed local/UTC parse = off-by-one class; PROOF as-of/period-end determinism; DOOR export timestamps. Note DST transition dates (America/Toronto) where a 23/25-hour day breaks day-arithmetic done via `+86400000`.
