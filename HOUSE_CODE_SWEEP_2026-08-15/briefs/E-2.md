# Slice E-2 — EXPO engine core (L12,074–26,910)

Read `_COMMON.md` first. Findings → `findings/E-2.md`. Repo: `/home/user/conc-kitchen-expo` (record HEAD sha).

**Attentive range: L12,074–26,910** — component job inference (@12074), the scheduling passes, fridge state simulation (@16162), day timeline (@19413), day detail (@20007), inline editing (@20621), send/chain generation (`generateSendRows` ~@14221), decision-ledger read/write paths in range, hub publish assembly (~@22428 — see note), save/snapshot machinery in range.

**This is the highest-stakes slice** — the engine that decides what gets cooked, where, and when, for ~185–200 shelter residents. Emphasis: footgun > data.

**Leads to deepen:**
- **Pass-ordering dependencies.** The KNOWN 2 send-model stragglers (Chicken Fajitas W1 SAT, Halal al pastor W4 TUE — hot-send instead of cold-ship) are a pass-ordering edge (~L14221 vs `fixSharedBasePinning`/holdDays passes). Don't re-report the stragglers themselves; DO map the scheduler's pass pipeline (order of passes; which passes read state an earlier pass mutates; which run before/after `reconcileSetDecisions` — a documented "keep SET-affecting passes before it" contract). Order-dependence with no enforcement is the finding class.
- **Copy-paste divergence between near-twin branches** — regular vs veg-alt vs halal emit paths (a documented historical bug class: guards added to one branch and missed in siblings). Diff the branches for guard asymmetries.
- **Food-safety re-derivation**: `holdDays` re-derivation on placement was a past must-fix (an advance add shipping without its cold round-trip). Verify current state; hunt sibling paths that copy hold/thermal fields verbatim instead of re-deriving from placement.
- `_coldChainHandled` rename completeness (was `_renoColdChainHandled`).
- Empty catches + silent failures inside scheduling passes (from the whole-file battery E-1 runs — inspect the ones in YOUR range yourself).

**Boundary note:** publish-path *atomicity/failure* analysis (`pushFilesToGitHubAtomic`, `publishHubSchedule` guard) is owned by slice X-B — note engine-side interactions (what triggers a publish, what state it snapshots) but don't duplicate X-B's depth.
