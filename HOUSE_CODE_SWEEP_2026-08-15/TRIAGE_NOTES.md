# TRIAGE_NOTES — HOUSE code sweep 2026-08-15

Raw intake: **93 findings** across 11 slices (per findings/ files; two slice summaries miscounted their own severities by 1 — the files are authoritative: X-B is 3 P1/6 P2/2 P3, E-3 is 3 P1/2 P2/2 P3).

## Cross-slice merges (dedupe — same defect found independently = convergence, boosts confidence)

| Merged ID | Members | Note |
|---|---|---|
| MERGE-1 | E-1-2 + E-3-6 | Day-Sheet stale `2026-05-10` anchor + no cycle wrap. Found independently by both EXPO agents. P1. |
| MERGE-2 | X-B-9 + X-A-5 | MISE 4-file CODEX publish non-atomic, zero retry (D1-class, different repo). P1. |
| MERGE-3 | X-A-3 + M-2-5 | The allergen-fabrication chain, both ends: data layer never uses `null` (12/240 `allergens:[]`, display's honest "unverified" state unreachable — M-2-5) AND the `|| []` fabrication reaches BOTH published feeds with a live empty-stub instance (Apple Crisp) consumed by DOOR slot-flag autofill + EXPO conflict-scan fallback (X-A-3). **The sweep's most consequential finding.** P1, latent. |
| MERGE-4 | H-1-8 + X-C-3 | `SECTION_COUNTS` write-only (3 writer pipelines + a dead DOOR registry fetch, zero readers). P2. |
| MERGE-5 | X-B-11 + X-C-2 | Shared `conc_gh_token` sanitized on read in EXPO only; DOOR/HUB read it raw (EXPO's fixed v9.29 crash class live in 2 siblings). P2. |
| MERGE-6 | D-1-5 + X-B-1 | DOOR publish-trigger sprawl (21 publishAndSync + 8 sidePublish sites, zero debounce) + un-busted SHA GETs making the 409 retry structurally ineffective; same-file double-writes guarantee the <60s window. KNOWN → D1, with substantial new mechanism evidence. P1 KNOWN. |

Post-merge: **87 unique findings** — **29 P1 · 39 P2 · 19 P3 · 0 P0**.

## Severity calls (main-loop calibration)

- **No P0s claimed and none upgraded.** Closest candidates examined: MERGE-3 (latent — none of the 12 empty-allergen recipes is on the live menu; verified by X-A) and X-C-8 (DOOR registry save silent-fail incl. anaphRooms — the same-session publish still ships correct data; the loss window is local-restore-after-failed-write). Both stay P1 with the latency/window stated plainly.
- D-1-3 (anaph banner GF-drop, KNOWN from the repo's own KNOWN_VS_SHOWN register, open ~8 weeks) + D-1-9 (the only covering test passes empty fixtures — the defect is structurally invisible to door-smoke's 70/70) together form the report's **top food-safety item**: known, open, and un-gated.
- Main-loop spot-verification (2026-08-16, this session): H-1-1 (deploy.yml lacks the sidecar cp — confirmed), E-1-1 (`_RAW_SEND_ARCHETYPES` const @10982 vs parse-time read @3985 via merge @9693 — confirmed), M-2-2 (`DOOR_LIVE_PUBLISH_APPROVAL` single assignment — confirmed).

## Refute set (per plan: 100% of P1s + all FORKs; P2/P3 calibration sample)

- **R-1 (HUB + DOOR + storage + portal):** H-1-1, H-1-2, H-1-3(FORK), H-1-4, H-1-5(FORK), D-1-1, D-1-2, D-1-3+D-1-9 (as one), MERGE-6, X-C-7, X-C-8, P-1-7(FORK), P-1-8(FORK) + calibration sample: H-1-6, H-1-15, D-1-7.
- **R-2 (EXPO + MISE + PROOF + cross):** E-1-1, E-1-5, MERGE-1, E-2-1, E-2-2(FORK), E-2-7, E-3-1, E-3-2, M-1-1, M-2-2, MERGE-2, MERGE-3, X-B-10, X-C-9, P-1-1, P-1-2 + calibration sample: E-2-3, M-2-6, X-A-4.
- Verdicts: CONFIRMED / REFUTED(reason) / DOWNGRADE / RECLASSIFY — appended below by the refute agents' reports.

## Verdicts (filled after refute pass)

### R-1 verdicts

Scope: HUB + DOOR + storage + portal. All findings re-grepped/re-read directly against the live repos at the SHAs cited in each finding (all confirmed unchanged at verification time). No git state changed.

- **H-1-1: CONFIRMED.** `deploy.yml`'s "Prepare Pages artifact" step (lines 36-53) copies `hub_schedule.json`/`hub_orders.json`/`hub_overrides.json`/`recipe_production.json` into `_site/` but has no `hub_schedule_version.json` line — confirmed byte-exact. `HUB_VERSION_URL`/`HUB_SCHEDULE_URL` both point at the same github.io origin the workflow deploys via `actions/deploy-pages@v4` (id-token/pages permissions confirm this is the real GHA-Pages mechanism, not branch-serving), so the sidecar genuinely 404s. Confirmed EXPO writes the sidecar every publish (`hub_schedule_version.json` is committed at HEAD, dated same as the swept commit) and the loader's fallback logic (`_fetchVersion`→null→"Step 2: legacy path") is real. No mitigating mechanism found. P1 justified — this silently kills a whole documented caching subsystem.
- **H-1-2: CONFIRMED.** `renderOverrides` (line 4842) interpolates `n.text`/`n.by`/`n.at` raw into a template literal with no `escH()` call, confirmed exact at line 4850-4851; the sibling general-notes card 200+ lines below uses a local `esc()` on the same data class — asymmetry confirmed. `HUB_OVERRIDES` is genuinely fetched from `hub_overrides.json` (multi-device shared, any token-holder writes via `addDayNote`). Cross-app token-theft claim holds: all HOUSE apps share one `kennedyjasondavid-eng.github.io` origin (confirmed EXPO's `getGHToken()` reads the same `conc_gh_token` key), so XSS here is a real cross-app credential-theft vector, not just local defacement. P1 justified.
- **H-1-3 (FORK): CONFIRMED.** The "SEEDED RECIPE LIBRARY" IIFE (lines 4890-4897) exists verbatim as quoted, including the `allergens: []` on several entries and the `if(!existing[k])` merge-only-missing-keys logic that reproduces deleted records on next load — confirmed by direct read. Cross-checked MISE's `allergenSummary()`: an empty (non-null) array genuinely grades `state:'clean'` ("No allergens detected — verify before service"), confirming the food-safety framing in consequence (c) is not speculative. Not overlapping with anything in KNOWN.md (grepped, zero hits). P1 as assigned is defensible given the cross-app second-writer + food-safety-adjacent allergen-grading angle; FORK direction is correct since the fix (delete vs. migrate) is a product call.
- **H-1-4: CONFIRMED, exactly.** `const SITES = ['Bloor', 'LAN'];` at line 5997 is the sole site-iteration source for `generateProductionCalendarXlsx`; computed the live `hub_schedule.json` census myself: `{Bloor: 337, Rex: 209}` with `Rex:COOK=95, Rex:PREP=16` and zero LAN items — matches the finding's cited numbers exactly (111 = 95+16 Rex jobs silently omitted). `ALL_SITES` (line 1292, from `HUB_HOME_SITES`) is confirmed as a real, already-used seam elsewhere. No mitigating mechanism — this export genuinely has no Rex sheet and no notice of the omission. P1 justified.
- **H-1-5 (FORK): CONFIRMED.** `compareBoardMenuToDoor` (line 1500) confirmed to iterate only `['lunch','dinner']`; live `hub_schedule.json` MEALS object confirmed to carry ONLY `lunch`/`dinner` keys across every entry (I ran a full-payload key census — zero veg/halal keys exist in MEALS at all, which actually strengthens the finding beyond what's stated: even widening the compare loop alone wouldn't help without also widening the EXPO writer). DOOR's live `menu_current.json` W1 TUE confirmed to carry `lunch_veg`, `dinner_halal`, `lunch_flags`, `allergens_lunch` etc. — the dimension genuinely exists upstream and is genuinely invisible to this check. The "matching content suppresses version-drift" gate-locked behavior is confirmed in the surrounding code/comments. Not overlapping with the KNOWN "detect-only" ruling (that's about remediation policy, not comparison completeness). P1 justified — this is a trust-surface overclaim on exactly the dimension (veg/halal/allergen) the sweep's own incident history says recurs.
- **D-1-1: CONFIRMED.** Zero `integrity=` attributes confirmed (grep). `downloadSpecialFile` (line 20420) calls `builders[type]()` bare, `buildAnaphListXlsx` (line 20190) starts with `XLSX.utils.book_new()` with no guard — confirmed a bare `ReferenceError` would propagate uncaught. Confirmed zero `window.onerror` in the file. Confirmed the guarded/unguarded split (`exportComplianceXlsx`:11162 and `handleImportFile`:18328 guard; `downloadSpecialFile`/`buildPlatingXlsx` don't). Confirmed `downloadAllFiles`'s catch produces the misdirecting "try downloading files individually" toast. One wording nuance: the libraries ARE version-pinned in their URLs (4.4.1/3.10.1/1.2.0) so "un-pinned" slightly overstates — the real gap is no SRI/no guard, which the finding's own evidence line states correctly. P1 justified (violates the repo's own "graceful degradation from file://" rule on the highest-stakes documents).
- **D-1-2: CONFIRMED.** Same code region as D-1-1 confirms this independently: the per-day try/catch in `buildPlatingXlsx` (lines 20082-20101) is console-only, `return wb.SheetNames.length ? wb : null` confirmed, `downloadPlatingSheet`'s `if (wb) XLSX.writeFile(...)` confirmed to have no else branch. If all 7 days fail, the click silently no-ops. No mitigating mechanism (no toast, no UI notice anywhere in this path). P1 justified on the same "silent gap in a food-safety document" grounds as D-1-1.
- **D-1-3 + D-1-9 (as one): CONFIRMED, thoroughly — no refutation possible.** Verified all four raw-tag-rescrape sinks (`r.tags.filter(t=>t.startsWith('No '))`) at lines 19921/19940/~20704/~20719, byte-exact. Confirmed `ANAPH_TAG_TO_MEAL_FLAG['GF']='hasGluten'` exists and — critically — `'GF'` is the ONLY key in that map that does NOT start with `"No "`, so the banner-text filter structurally cannot ever include it: this is a deterministic bug, not merely probabilistic. Confirmed `getAnaphConflictRooms`/`getAnaphAlertInfo` (lines 9121/9147) correctly handle GF via `anaphTags`+the map, and are NOT what the banner text reads (banner re-derives independently). For D-1-9: confirmed byte-exact that the only `buildPlatingHtml` test passes `conflictRooms: new Set(), anaphSect: []` (door-smoke.mjs:1146-1147), so the buggy branch never executes; confirmed `getAnaphConflictRooms` appears exactly once in the whole suite as a bare string in a `safetyMarkers` census array, never invoked; confirmed zero references to `getAnaphAlertInfo`. Ran `node --test tests/door-smoke.mjs` myself: 70/70 green, confirming the "green CI, live defect" framing is real, not asserted. Also confirmed the KNOWN_VS_SHOWN_FINDINGS.md C1 register genuinely predates this (2026-06-21) and the "M5 half-fixed" sub-claim is exactly right down to matching line numbers (`buildMenuJSON`/`buildRoutingByMealJSON` use `DOOR_SCHEMA_VERSIONS.*`; `buildStateJSON`/`buildRegistrySummaryJSON` still hand-type `version:30`). This is legitimately the sweep's top food-safety item as the main-loop already flagged. P1/status KNOWN both correct.
- **MERGE-6 (D-1-5 + X-B-1): CONFIRMED**, with every cited line number in X-B-1 verified exact (`:12150`/`:12178` SHA-GET+retry with no cache-buster, `:12343`/`:12347`/`:12348`/`:12349` wave slots, `:14042`/`:9276`/`:17589`/`:17601` sidePublish sites — all match precisely). Traced the mechanism myself: `sidePublish`→`ghPushFile`→`_ghWriteQueue`-chained→`_ghPushFileNow`, and the wave's `_push` goes through the identical `ghPushFile`/`_ghWriteQueue`/`_ghPushFileNow` path — confirming same-tab serialization genuinely cannot prevent the HTTP-cache poisoning (the queue only orders the writes, it doesn't bust the browser's 60s cache on the SHA re-GET). **One evidence inaccuracy found:** the finding says `validateToken`'s `cache:'no-store'` precedent sits "55 lines above [`_ghPushFileNow`'s SHA-GET], in the same object" — false on both counts: `validateToken` is a `PublishAuth` method (line 11337+) while `_ghPushFileNow` is a separate top-level function (line 12139), ~660 lines apart, not 55, and not the same object. The core claim (an in-house `cache:'no-store'` pattern exists and isn't reused here) is still true and doesn't depend on that phrasing — but the specific citation is wrong and should be corrected to "elsewhere in the file" in any final report. Status KNOWN→D1 correct.
- **X-C-7: CONFIRMED.** `saveHubConnect()` (line 3746) confirmed byte-exact: `if(token){...}else{removeItem('conc_gh_token');removeItem('hub_gh_token')}`, no `confirm()`. Field markup confirmed `type="password"`, hint text matches the quoted evidence verbatim. Confirmed 0 hits for "disconnect" anywhere in the HUB file. Confirmed DOOR's `forgetToken(confirmFirst)` (line 11514) genuinely gates on `confirm()` — a real in-house reference pattern, though it protects DOOR's own separate `appSettings['gh-token']`, not `conc_gh_token` directly (DOOR only reads the shared key once, for one-time migration) — a minor nuance the finding doesn't spell out but doesn't undermine the comparison. Confirmed EXPO's `getGHToken()` genuinely falls back to reading the same `conc_gh_token` key, substantiating the cross-app blast-radius claim. P1 as assigned is reasonable given the shared-credential/publish-chain angle, though this is arguably borderline P2 since the finder's own text notes the resulting symptom (EXPO publish failures) surfaces loudly elsewhere — I would not push back hard on either P1 or P2 here.
- **X-C-8: CONFIRMED, but I found a real mitigating mechanism the finder did not account for — flagging for the report's consequence text, not disputing the underlying defect.** `saveRegistryState()`'s `catch(e){console.warn(...)}` confirmed exact (line 9782-9789); confirmed 23 real call sites (finding says "24" — appears to count the function's own definition line, which also matches the literal grep pattern; off-by-one, immaterial). The cited anaphylaxis revert-path example (line 7930-7938) confirmed exact. However: I traced DOOR's boot sequence and found `bootRegistry()`'s `loadRegistryState()` (local-first) is followed by an **unconditional** `setTimeout(()=>{ensureRegistrySynced();},500)` on every page load (line 19415, top-level, not gated), which calls `pullStateFromGitHub(false)` — this has a provenance-freshness guard (`getRegistryProvenanceTs()` vs the cloud's `_meta.exported`) that, when the cloud is newer, **overwrites the stale local copy** with the correct data. Since `publishAndSync` (called right after the failed save, per the finding's own cited example) reads live in-memory `REGISTRY_LIST` (not the failed localStorage write) and DOES succeed in publishing the correct data to the cloud in the same call chain, and the revert-path example specifically does NOT call `setRegistryProvenance()` (confirmed by grep — no hits near lines 7900-7940), the stale local provenance timestamp would correctly read as older than the cloud's freshly-bumped `_meta.exported` on the next boot, and the auto-sync would self-heal within 500ms of the next network-connected reload. This substantially narrows the "later reload... silently reverting" framing — the real exposure window is: offline at both edit-time-publish AND next-reload-time, or a crash before ever completing a network-connected reload. That said, I do NOT recommend a hard downgrade: the underlying code defect (silent catch, zero user-facing acknowledgement) is still real and still violates DOOR's own stated rule that anaphylactic flags must be "always surfaced in red with explicit acknowledgement" — the self-heal, even when it works, is silent, not an acknowledgement. P1 stands, but the consequence section should note the self-heal mechanism exists and narrows (not eliminates) the risk.
- **P-1-7 (FORK): CONFIRMED.** `GATED_APPS` (line 357), all three `pinHash` values confirmed 64-char valid hex via direct check; all three plaintext `url:` fields confirmed sitting a few lines above their hashes in the same object literals. `verify()` (line ~437-463) confirmed to have zero lockout/rate-limit — exact shake+420ms-reset behavior verified. HUB's card confirmed to be a plain unguarded `<a href>`. One minor imprecision: the finding says a "three-line console script (`GATED_APPS[0].pinHash` is public)" could extract the hash, but `GATED_APPS` is declared inside an IIFE closure (`(function(){'use strict';var GATED_APPS=[...]...})()`), not exposed on `window` — so it is NOT directly accessible from the browser console as literally described. The hash is still trivially visible via View Source (functionally equivalent for a "source-reading attacker," which is the finding's own broader framing), so the substance of the point stands; only the specific "console script" mechanism claim is technically wrong. P2/FORK as assigned is right — this is a known≠shown labeling issue, not a new vulnerability (the apps have no other access control either way).
- **P-1-8 (FORK): CONFIRMED.** Confirmed 0 hits for "conc-kitchen-proof" in HOUSE's `index.html`; confirmed the 4-card grid (DOOR/CODEX/HUB/EXPO only, lines 280-316) has no PROOF card. HOUSE's own CLAUDE.md confirmed to list PROOF as one of the five apps. The finding's own severity/confidence framing is appropriately cautious (P3, explicitly flags it might be intentional, explicitly couldn't verify Pages-enablement for PROOF) — no grounds to escalate or refute; this is a well-calibrated, honestly-scoped finding.

**Calibration sample:**
- **H-1-6: CONFIRMED, severity P2 as assigned.** Confirmed `let TODAY_INFO=getTodayInfo()` (line 3022) is module-scope/boot-only, confirmed recompute only inside `_loadScheduleDataImpl` (line 4610), confirmed zero `setInterval` in the file, confirmed the only triggers are boot/visibilitychange/retry-click. No other periodic recompute mechanism found (checked for `requestAnimationFrame`/other focus listeners — none). The "worse across the 28-day wrap boundary" claim holds up: `projectDates()` (the whole calendar re-projection, not just the TODAY ring) is gated behind the same trigger set, so a boundary-crossing always-on kiosk would show a fully stale date mapping, not just a wrong ring. P2 is right — annoying/misleading but the actual schedule content (frozen `dateNum`/`week.number` join keys) stays correct, so no food ever goes to the wrong day because of this specifically.
- **H-1-15: CONFIRMED, severity P2 as assigned, byte-exact.** `renderCostPanel`'s guard (line 3570, `!HUB_ORDERS||!HUB_ORDERS.byWeek`) confirmed to be an existence check; live `hub_orders.json` confirmed verbatim `{"_source":"placeholder...","byWeek":{},"byDay":{}}`; confirmed `HUB_ORDERS = await ordRes.json()` (line 4576) has no shape validation and `openCost()` has no guard of its own before calling `renderCostPanel`. Confirmed `Math.max.apply(null,[])`→`-Infinity` is real but genuinely unconsumed (the `wks.forEach` loop that would read it never runs on empty `wks`) — the finder's own "latent trap, not live" framing is accurate, not overclaimed. Correctly distinguished from KNOWN H2 (the pairing gap) as a separate rendering bug the pairing gap merely exposes. P2 right — cosmetic/misleading, not a food-safety miss.
- **D-1-7: CONFIRMED, severity P2 as assigned.** Programmatically parsed `DOOR_RECIPE_DATA_FALLBACK` myself: exactly 131 records, all `stream:"regular"`, 0 vegan — matches exactly. Parsed the live `conc-recipe-hub/DOOR_RECIPE_DATA.json`: exactly 240 records, `{regular:167, vegan:73}` — matches exactly. Confirmed `getVegAltAllergenStr` (line 16395) returns `''` unconditionally when `veganRecipes.length===0`, and confirmed `recipeMatchesSlotDef`'s stream filter would likewise return zero results for a vegan-streamed slot against the fallback. Worth noting for the report: the finder's own text already correctly flags this as "safe direction" (falls back to the broader main-meal allergen set, which DOOR's July-18 CLAUDE.md entry documents as the safe direction for this exact lookup) — this is a determinism/staleness bug, not a safety regression, which is presumably why it's P2 and not P1. Correctly scoped as distinct from KNOWN DOOR #63 (that's the matcher's live-data narrowness; this is the fallback tier missing the vegan dimension entirely).

### R-2 verdicts

Scope: EXPO + MISE + PROOF + cross-app. Every finding re-grepped/re-read directly against the live repos (EXPO `2686de6`, MISE `6ea8b30`, DOOR `7500521`, PROOF `b3dc772` — all confirmed unchanged at verification time). Every quantitative claim (byte counts, record counts, line numbers) was independently recomputed from scratch with fresh scripts/greps, not copy-checked against the finder's numbers. No git state changed.

- **E-1-1: CONFIRMED, P1, deep-dive per instructions.** Traced the full chain myself: `:3985` bare (unguarded) read of `_RAW_SEND_ARCHETYPES` inside `compileProductionFacts`'s Path-4 branch; `const _RAW_SEND_ARCHETYPES` declared `:10982`, ~7000 lines below; parse-time merge `Object.entries(_customRecipes).forEach(...)` at `:9693` is a bare top-level statement (confirmed no enclosing `<script>` break or wrapping try/catch from `:2061` onward) that reaches `_materializeCustomRecipe`→`compileProductionFacts` with zero guards; confirmed `_customRecipes` is populated from `localStorage['pp_custom_recipes']` at parse time (`:3125-3126`); confirmed zero `window.onerror`/global recovery exists, so an uncaught exception here aborts the whole script before `boot()`/`init()` (`:39554`) ever runs — reproducing the exact v9.49.28 blank-board class. Confirmed both cited gates miss it: `boot_schedulemode_order_gate` only pins the `scheduleMode` declaration; `populated_boot.json`'s custom recipe has `steps.length===1` (never reaches Path 3/4); I additionally found `production_facts_compiler_gate.mjs` *also* can't catch it even though it exercises `compileProductionFacts` directly — it calls the function post-boot (after `_RAW_SEND_ARCHETYPES` is already initialized) and never even constructs a 2-step `steps` array, so Paths 3/4 aren't exercised at all by that gate. **Mitigating nuance I found that the finder didn't distinguish (narrows, doesn't refute):** I traced all three in-app writers of `pp_custom_recipes` and found the "New Item Wizard" (`_wizFactsRecord`, `:37769+`) can only ever emit a single-step `sameDay` record (no "advance" timing exists in its UI), and the "Describe" kitchen-noun templates (`_ND_TEMPLATE_PRESETS`, `:36978-36998`) are 4 hardcoded presets — one is 2-step advance-cook→heat (Path 3, safe, since Path 3 only reads `CHAIN_ARCHETYPES`, declared safely early), none is the hazardous 2-step prep→cook (Path 4) shape; `confirmAddJob`'s "Remember this item" writes a legacy-shaped entry with no `productionFacts` field at all (bypasses the compiler entirely). So none of today's three in-app *authoring* paths can currently produce the crash. The live vector is `importUserData()` (`:26801-26807`), which accepts arbitrary externally-sourced `customRecipes` JSON unchecked and calls `rememberCustomRecipe` directly — genuinely reachable, plus the landmine reactivates the instant any future preset/wizard update adds the already-named-elsewhere `rawFishSend`/`rawTofuSend`/`rawLegSend` pattern (these archetypes are already live production reality per RECIPE_DB entries the app schedules today). Recommend the finding's "first operator who saves such a recipe" framing be qualified to "via import or a future preset" rather than implying today's Describe/Wizard UI reaches it directly — but the underlying code hazard, its blast radius, and the gate-blind-spot claim are all fully verified real. Severity P1 stands.
- **E-1-5: CONFIRMED, P1.** Both write sites verified near-exact (`:22702-22707` read guard, `:22764` write — one line off from the finding's `:22763`, trivial): `try { localStorage.setItem('pp_last_published_version', boardVersion); } catch(e) {}` with the comment above it matching verbatim. Confirmed `setItemSafe()` exists at `:31534` exactly as described (try→on-quota `ensureStorageHeadroom()`→retry→`console.warn`), so the "existing seam" direction is real and unused here. Consequence chain (stale guard → next auto-publish trigger re-pushes unchanged content → reproduces the v9.31.11 duplicate-publish/SHA-race class already logged live for DOOR in KNOWN.md) is logically sound. This is the most *contingent* of my assigned P1s — it requires `localStorage` to already be at quota specifically when this small write fires — but shared-origin quota pressure is a real, repeatedly-documented HOUSE condition (the entire Save-trust PR-A→D arc exists because of it), so "a user CAN hit this" is satisfied. No downgrade.
- **MERGE-1 (E-1-2 + E-3-6): CONFIRMED, genuine independent convergence.** Both slice files cite byte-identical evidence (`:33631`/`:33691` — my own grep confirms these are the exact lines), the same two helper names (`dayDateLabel`/`getWeekDateRange`), and the same `HUB_CYCLE_START` contrast (confirmed `= new Date(2026,5,7)` at `:21756`). Confirmed the Day Sheet view button is live top-bar UI (`:1708`, `view-switch` group) and the print button wires to `printDaySheet` (`:33700`) which reads the same header. Independently verified the UTC-parsing off-by-one claim is technically correct (`new Date('YYYY-MM-DD')` parses as UTC midnight; in Toronto this renders one calendar day early). No mitigation found — this is a 100%-guaranteed-wrong, routinely-visible date label. P1 fully justified, arguably the single most certain-to-manifest finding in my whole set (every view/print of that screen is wrong, no trigger condition needed).
- **E-2-1: CONFIRMED, P1, correctly self-scoped.** Verified the exact pass sequence (`generateSendRows`→`fixCompanionTiming`→`fridgeReliefPass`→`fixSharedBasePinning`→`earlySendPass`→`reconcileAdvanceColdChain`) and its self-documenting comment matches the evidence quote in substance. Grepped `tests/*.mjs` myself for any ordering/`indexOf` assertion on these pass names — zero hits, confirming "no gate pins any of these orderings." The finding correctly discloses the concrete 2-straggler manifestation is already KNOWN and scopes its NEW claim to the enforcement gap itself, which is accurate triage, not overclaiming. P1 defensible given the class has already produced one real, live, currently-active defect (proof it's not hypothetical) and the codebase is under continuous heavy edit in exactly this region.
- **E-2-2 (FORK): CONFIRMED, P1, FORK correctly framed.** Confirmed `MODE_PROFILES` (`:2125`) now has exactly ONE key, `standard`, with `activeSites:[BLOOR,REX]` — no `reno` entry exists anymore, so `isLANActive()` is unconditionally `false` today (even stronger than the finding's "false in standard" framing — there is no other mode to fall into). Verified both gate lines (`:15972`, `:16077-16078`) and pipeline call sites (`:14861-14862`) exact. Independently recomputed the baseline census from scratch (not trusting the finder's numbers): `tests/ux_baseline.json` has exactly 532 rows, exactly 5 Bloor COOKs of FROZEN_STOCK_DB-registered items (`Pork Sausages`×2, `Halal beef sausages`, `Halal Turkey Sausage`, `Crispy Chicken Tenders`) and exactly 0 rows of type `PULL` anywhere — a perfect, independently-derived match. This is airtight data; the only reason it's a FORK and not a clean bug is the genuine product ambiguity (does standard Bloor still source from the Rex freezer or not) — correctly left to Jason. No refutation possible.
- **E-2-7: CONFIRMED, P1, no mitigation found despite specifically looking for one.** Read `completeSwap`'s full body (`:23380-23429`): direct `schedule[week][day]` array splice, hand-rolled `((svcIdx - linearIdx(...)) % 28 + 28) % 28` holdDays formula (matches evidence), `trackEdit()` confirmed by source to be `_editLog.push()` + `renderDiffBadge()` only — no `_ledger`/`makeDecision`/localStorage write anywhere in it. Confirmed zero call to `runScheduler()` anywhere in `completeSwap`. Confirmed the contrast: `deriveHoldDays()` exists (`:30317`) and `moveItemToDay` uses it (`:20498`, with an explicit "Slice 3b" comment), and `.hold-stop-overlay` is a real, separately-gated hard-confirm mechanism (`:1032` CSS, `:33490` usage) that `completeSwap` never references. Confirmed the Swap button is a live, first-class edit-panel verb (`:26252`, sibling to Move/Pin/Remove/Reset). I looked for — and could not find — any later reconciliation pass that would fold a raw swap into the Ledger before the next Generate; per the app's own documented architecture (`schedule` is rebuilt from menu+Ledger every Generate), the swap is provably discarded. Textbook "my edit disappeared" class. P1 fully earned.
- **E-3-1: CONFIRMED, P1, airtight — verified on the actual committed artifact, not just source.** Both hardcoded literals found at the exact cited lines (`:26746` = `'v9.49.12'`, `:31226` = `'v9.8'`). `APP_VERSION` confirmed `'v9.49.36'` at `:2068`, exactly as claimed. Directly opened `snapshots/latest.json` myself: `version: "v9.8"`, `ts: "2026-08-10T21:37:33.660Z"` — both fields match the finding's quoted evidence character-for-character. Import-toast consumer confirmed at `:26879` (one line off from the cited `:26880`, trivial). No mitigation exists — every snapshot/export ever produced by this app carries a wrong provenance stamp, permanently, until fixed. Whether P1 vs P2 is a close call given the low daily-operational (non-audit) reach, but "trust/provenance defect" is explicitly named in the P1 class rubric and this is unambiguously that — no downgrade.
- **E-3-2: CONFIRMED, P1, with one clerical note for the report.** Verified all 9 named gate files exist on disk as substantial files (sizes 4.9KB-18KB). Extracted the CI `verb-gates` job's full `for g in ...` enumeration myself: exactly 131 `tests/*.mjs` paths (matching the finding's own "131 entries" figure precisely) — and confirmed all 9 named gates (7 real-drift + `jerk_tofu_menu_parity_gate` + `hub_stage_nocard_cross_repo_gate`) are absent from it. `jerk_tofu_menu_parity_gate`'s KNOWN status is confirmed verbatim against KNOWN.md. **Clerical note:** the finding's own title and "where" line correctly enumerate **seven** gate files, but its "status" and "direction" lines both say "the other **six**" / "add the **six**" — an internal off-by-one inconsistency within the finding itself (not a factual error about the repo — I independently confirm all 7 are real and all 7 are absent from CI). Worth a one-word fix in the final report but doesn't change the verdict.
- **M-1-1: CONFIRMED, P1, independently re-verified live per instructions (did not just trust the prior agent's transcript).** Re-ran the CORS preflight myself against the real `api.anthropic.com` endpoint: without `anthropic-dangerous-direct-browser-access` in the requested header list → `HTTP/2 400`, no `access-control-allow-origin` header at all (real Cloudflare/Anthropic response, confirmed by `cf-ray`/CSP headers matching Anthropic's actual signature). With it added → `HTTP/2 200`, `access-control-allow-origin: *`. Byte-identical result shape to the finder's claim. Confirmed both exact call sites (`:16716`, `:22761`) send only `Content-Type`/`x-api-key`/`anthropic-version`, and confirmed the file contains zero occurrences of the opt-in header string anywhere. Traced the in-app consequence directly in source: `translateSmart`'s `try/catch` (`:16716-16736`) does fall back to `translateDumb` with the exact quoted "Smart parse failed…" message, and `testApiKey`'s `catch` (`:22764`) does produce the exact quoted "✗ Request failed" text — both fire because a browser CORS failure makes `fetch()` itself reject (TypeError), which both catches correctly. Confirmed `translateSmart` is wired to a real, live Settings→AI "Smart Mode" toggle (`:4627-4628`), not dead code. Every claim in this finding independently reproduced from first principles. No refutation possible; P1 fully earned (100%-reproducible complete feature failure with actively misleading error copy).
- **M-2-2: CONFIRMED, P1, full chain traced end-to-end.** Confirmed `DOOR_LIVE_PUBLISH_APPROVAL` (`:30569`) has exactly one assignment in the whole file (grep `=` — one hit, its own declaration; corroborates the main-loop's independent spot-check noted in this file's Severity-calls section) with `approvalStatus:'pending', approved:false`, never reassigned. Confirmed `_cxDoorLivePublishApprovalStatus` (`:31042-31047`) requires both to flip before `approvalRecorded` is true, so it always pushes its blocker. Confirmed `buildPublishDryRun` (`:32293`) merges `doorPublishGuard.blockers` into its top-level `blockers`. Confirmed `publishRecipeProduction` (`:32656-32667`) early-returns "Publish blocked" whenever `blockers.length>0`, with zero bypass — checked both of its two call sites (`:22996` button, `:28598` silent auto-call) and neither passes any override. Confirmed the "Publish Now" button styling/label quote (`:22996`) matches exactly. No mitigating mechanism exists; the button is unconditionally, permanently dead. P1 fully earned.
- **MERGE-2 (X-B-9 + X-A-5): CONFIRMED, genuine independent convergence.** Verified `publishRecipeProduction`'s payload array is exactly the 4 files claimed (`recipe_production.json`, `DOOR_RECIPE_DATA.json`, `recipe_production_meta.json`, `DOOR_RECIPE_DATA_meta.json`) pushed via a sequential `for` loop, each `await ghPushFile(...)` with no wrapping transaction. Read `ghPushFile` (`:28604-28614`) in full: single SHA-GET (silently swallowed on failure), single PUT, throws immediately on failure — zero retry logic anywhere, confirming X-B-9's sharper claim exactly. Both slice agents independently found and described the same underlying mechanism (X-A from an atomicity angle, X-B from a food-safety pairing-skew angle) — high-confidence convergence, not a coincidence of vague description. P1 justified on the same grounds as the DOOR-side D1 KNOWN item this is adjacent to.
- **MERGE-3 (X-A-3 + M-2-5): CONFIRMED, P1 — and the "sweep's most consequential finding" framing is earned, not just asserted.** Independently rebuilt M-2-5's entire census from scratch with my own Node script (not trusting the finder's numbers): 240 records, `{missing:0, null:0, emptyArray:12, nonEmptyArray:228, nonArrayType:0}` — exact match including the full list of 12 recipe names. Verified `allergenSummary`'s 3-state logic (`:14249-14254`) matches verbatim. For X-A-3: directly opened the checked-in `DOOR_RECIPE_DATA.json` (240 records) and confirmed `"Apple Crisp"`/`"Apple Crisp Dessert"` both publish `allergens:[]` live today; pulled the full source seed record for "Apple Crisp" from `CONC_Recipe_Data.js` and it matches the finder's quoted evidence character-for-character (empty ingredients/instructions, `status:"production"`, same `promotedBy`/`savedAt`). **One citation correction, doesn't weaken the finding:** the finder's DOOR-side line (`:14271`, inside `resolveFlags`) is real code with the exact quoted text, but that function actually serves DOOR's meal-swap/special-meal resolver (`swLunch`/`smLunch`), not the primary CODEX recipe-link autofill. I traced the *actual* recipe-link path myself (`existing.linkedRecipes` restore, `:17041-17044`) and found it calls `hubAllergenToFlags(recipe.allergens)` (`:16218`), which does `(allergens||[]).forEach(...)` over a flags object pre-initialized all-`false` — the exact same masking defect, on the exact path a supervisor would actually use to link a CODEX recipe to a menu slot. So the safety claim is not just confirmed but *strengthened* by a more central second instance the finder's citation didn't land on. Also confirmed `buildRecipeProductionJSON` (`:28629-28655`) carries the identical `entry.allergens||[]` line feeding the separate `recipe_production.json` feed, corroborating the "second propagation path" claim. This is a real, live, dual-feed, dual-app latent food-safety-adjacent data-integrity defect. No refutation attempted survives the evidence; P1 stands, and I'd support the sweep's characterization of it as the top finding.
- **X-B-10: CONFIRMED, P1.** Verified all 5 cross-app fetch call sites (`:35272`, `:35682`, `:36089`, `:36364`, `:36391`) are bare single-argument `fetch(url)` — confirmed via direct read of each, not just grep context — with zero `cache:` option and zero query-string busting anywhere near them. Verified the contrast: HUB's `_fetchJSON` (`:4260-4268`) genuinely does both `cache:'no-store'` and a `?t=` busters. No mitigation found (no other cache-defeating mechanism exists upstream of these calls). P1 justified — this is the one hop in the DOOR→EXPO→HUB pipeline with zero transport protection despite EXPO's own extensive, separately-documented effort on menu-freshness *logic*.
- **X-C-9: CONFIRMED, P1.** Confirmed exactly 28 `setItem(RECIPE_LIBRARY_KEY,...)` sites and zero occurrences of "quota" (case-insensitive) anywhere in MISE's `index.html` — both exact matches to the finding's claims. Spot-checked 2 of the 4 sampled sites directly: `:22215` (delete-recipe) and `:24666` (rename-propagation) are both genuinely unwrapped, and the delete-recipe site does mutate `lib` in-memory (`delete lib[name]`) before the unguarded `setItem`, exactly as described — a throw there would abort before `updateRecipeLibraryBadge()` runs. Given X-C-9 is essentially the MISE-specific instance of the same "no quota awareness" class already well-evidenced elsewhere in this sweep (EXPO's whole Save-trust arc exists because of it), the consequence framing (silent recipe-save failure discovered only on reload) is plausible and consistent with documented HOUSE-wide storage pressure. P1 stands.
- **P-1-1: CONFIRMED, P1, every quantitative claim independently reproduced from scratch.** `manifest.json`'s "Captured 2026-06-15" note confirmed verbatim. `routing_by_meal.json` fixture `_meta`: `exported:"2026-06-11..."`, `version:30` — confirmed exact; live DOOR routing: `exported:"2026-08-15..."`, `version:31` — confirmed exact. `recipe_production.json` fixture: `generated:"2026-05-08"`, 224 recipes — confirmed exact; live MISE feed: `generated:"2026-07-28"`, 240 recipes — confirmed exact (arithmetic: 16 recipes / 2.5 months of drift, matches). Independently checked the plant-forward masking claim for all 3 named dishes: `Couscous`/`Rice and Beans`/`Garlic Bread` are all `stream:"regular"` (both top-level and `tags.stream`) in the fixture but `stream:"vegan"` live in MISE today — exact match. `hub_schedule.json` fixture confirmed `_mode:"reno"`, `_cycleStart:"2026-05-10"`, and `_schemaVersion` key absent entirely. This is about as thoroughly verifiable a finding as this sweep produced; no refutation attempted survives.
- **P-1-2: CONFIRMED, P1.** My first grep for `"|| '2026-05-10'"` (with a space) found only 5 of the claimed 8 sites; re-grepping without assuming whitespace found the missing 3 use a compressed `&&`/`||` form with no spaces — all 8 cited line numbers (`1559,1639,1670,1859,1931,1986,2031,2236`) confirmed exactly once I accounted for that formatting variance. Confirmed `P.validateSchedule` (`:401-410`) computes `ok:!flags.some(f=>f.level==='error')` and pushes a `hub.cycleStart` `error`-level flag when `_cycleStart` is missing. Confirmed `tier1Present()` (`:1556`) gates only on `.prov.tier`, never on validation results. Traced the actual call site (`loadSource`, `:1362-1363`): `vr = P[s.validate](data)` then only `vr.flags`/`vr.version`/etc. are read into `DATA[key]` — `vr.ok` is computed and then genuinely never referenced again anywhere in the file (confirmed via a repo-wide grep for `vr.ok`, zero hits). The finding's own framing is appropriately non-overclaiming: it correctly notes `vr.flags` DOES get folded into a generic "N problems" banner, so the specific claim is "no *specific* signal blocks/greys the *numbers*," which is exactly what I verified. No mitigation found; latent-but-real, matches the finder's own honest "does not fire today" framing. P1 stands given HUB was bitten by this exact defect class once already (documented, cited correctly).

**Calibration sample:**
- **E-2-3: CONFIRMED, P2 as assigned.** Verified `hasSend`'s exact `.substring(0,15)`+`.includes()` dedup (`:16744-16745`) and the separate `generateSendRows` dedup (`.substring(0,20)`+`.includes()`, `:15610-15614`) — two independent instances of the identical anti-pattern, confirming "4+ sites." Confirmed `_suppressAutoSend` exists at 4 sites (`:13461`,`:15491`,`:16695`,`:17100`) as the documented workaround. Confirmed `cascadeSiteChange` (`:20695`) is genuinely uncalled anywhere else in the file (dead code) yet still carries the same substring-dedup pattern at its own `:20757` — correctly characterized by the finder as evidence the pattern is "still live" as an anti-pattern even where the specific function is dormant. No refutation found.
- **M-2-6: CONFIRMED, P2 as assigned.** Independently recomputed the mutual-exclusivity split from scratch with my own script: `{both:0, topOnly:93, prodOnly:147, neither:0, total:240}` — an exact match to the finding's claimed 93/147/0/0 split. Confirmed `buildRecipeProductionJSON` (`:28616-28655`) reads exclusively `entry.production` (aliased `p`) for every production field, with zero reference anywhere in the function to the top-level `equipment`/`primaryTemp`/`sourceSite` triple. The finding's own honesty that this is latent-not-live (the checked-in `recipe_production.json` is fine today because it's built from the live merged library, not the static seed) is correct and appropriately hedges the severity down to P2. No refutation found.
- **X-A-4: CONFIRMED, P2 as assigned — and I tightened the evidence further.** Confirmed `revertToCanonical` (`:32435+`) explicitly folds `permanentOverrides`/`sessionOverrides` (Slice 1) and `dbPatches`/`userMealOverrides` (Slice 2) with visible commented code, and contains zero reference to `s.editedItems` anywhere in its body. My first count-check under-matched (5 of 6 files found via an incomplete date-range glob); re-globbing more broadly surfaced a 7th legacy file (`snapshots/2026-05-16/13-20-50_jj4ui5.json`) the finder's own count already correctly included — the true picture across all 7 legacy-era snapshot files is `dbPatches` constant at 2 in every one, `editedItems` at 36 or 37 in exactly 5 of them and 0 in the other 2 — an exact match to the finder's "7 files... 5 of them" claim once I accounted for my own initial gap. Confirmed zero snapshot from 2026-06 onward carries an `editedItems` key at all, supporting the "bounded to these specific old files" framing. No refutation found; if anything this finding under-claims slightly by not naming the specific 7th file, but the aggregate numbers are exactly right.
