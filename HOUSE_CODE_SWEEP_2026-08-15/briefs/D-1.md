# Slice D-1 — DOOR full app + gate-coverage map

Read `_COMMON.md` first. Findings → `findings/D-1.md`. Repo: `/home/user/conc-kitchen-door` (record HEAD sha).

**MANDATORY pre-reads:** `DOOR_POST_DEFUSE_REMEDIATION_HANDOFF_2026-08-15.md` (this week's work — its territory is KNOWN) + KNOWN.md's DOOR section. Then `git log --since=2026-08-09 --stat` — defuse-merge hunks are skim-class.

**App:** `index.html`, 21,553 lines — resident registry + menu source + plating/compliance engine. Upstream of everything; **the highest food-safety stakes in HOUSE** (anaphylactic routing, allergen labels, halal/vegan compliance). localStorage = live state; committed JSONs = published snapshots.

**Attentive: the full file** (CSS → @689 sampled only). Specific attention:
- **Head scripts @689–691** (Chart.js / jszip / xlsx): CDN-loaded? SRI-pinned? What degrades offline / if the CDN is down — vs the "graceful degradation from file://" rule. (EXPO/HUB claims vs DOOR's reality.)
- **Anaphylactic + allergen net (known≠shown lens, top priority):** the enforcer predicates (`getAnaphConflictRooms`, routing lockout, plating ALERT) vs every *surface* that displays allergen/anaphylaxis state — does each surface re-derive from the enforcer, or re-scrape raw tags (a documented past finding: the red banner re-scraping raw resident tags, empty `()` on GF-only)? The repo has a `KNOWN_VS_SHOWN_FINDINGS.md` — read it and verify which items are STILL open at HEAD (each still-open item = a KNOWN finding with fresh evidence; each fixed one = confirmed-healthy).
- **Fuzzy-match overreach siblings:** DOOR #63 (`getVegAltAllergenStr`) is KNOWN — hunt SIBLINGS: `importLevenshtein` @17828 (used @18008) and any other fuzzy/substring matcher whose wrong match changes an allergen/routing outcome.
- **Menu representation layering** (`concUploadedMenu` base / `concMenuBase` delta / overlay / `standardCutover` pruning): the fresh design doc `DOOR_MENU_REPRESENTATION_FOOTGUN_DESIGN_2026-08-14.md` owns this territory (KNOWN) — sweep for footgun classes it does NOT cover; don't restate it.
- **Publish path:** serialization (`_ghWriteQueue`), Gate-9 preflight, stale-tab guard — the Aug-15 409 race is KNOWN D1 evidence; X-B owns publish depth. You own app-side: what *triggers* publishes (every editor save auto-publishing — count the trigger sites), and any publish-adjacent silent catch.
- 34 empty catches — triage load-bearing ones. 160 innerHTML sinks — the 2026-06-18 hardening claimed every display sink escaped; verify sinks added SINCE June, and spot-check 10 pre-existing ones.
- **Print stylesheet in full** — plating sheets are printed food-safety documents; verify the print path can't drop allergen/ALERT content (`@media print` display rules vs the ALERT/flag elements).

**door-smoke.mjs (2,289 lines): gate-coverage MAP only** — which invariant families the ~70 gates cover vs which load-bearing behaviors are ungated (e.g. publish trigger paths? overlay merge? veg-alt lookup?). Don't read every test body. Note the CLAUDE.md claim that the overlay gate is "not run under node --test — CI is the first real execution" — verify current harness reality.
