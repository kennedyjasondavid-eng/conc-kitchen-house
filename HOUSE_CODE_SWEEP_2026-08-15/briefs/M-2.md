# Slice M-2 — MISE/CODEX back half (L18,141–36,482) + data layer by script

Read `_COMMON.md` first. Findings → `findings/M-2.md`. Repo: `/home/user/conc-recipe-hub` (record HEAD sha).

**Skim rule (run FIRST):** `git log --since=2026-08-09 --stat` — Aug 10–12 merge hunks are skim-class.

**Attentive range: L18,141–36,482**, in this order:
1. **The unbannered tail 27,061–36,482 FIRST** (least-reviewed code): identity map/sidecar machinery, publish path @~32722, release/promotion surfaces.
2. Settings/site profiles @20466, SharePoint + Graph @24240 (incl. `graph.microsoft.com/v1.0/me/sendMail` @24407 and the second `api.anthropic.com` site @22761 — mechanism + key storage + failure mode + sink safety), recipe log @24457, variant wizard @26684.
3. Publish-path *ceremony vs boundary* (lens 9): per-record `publishAuthorized` blockers, dry-run checks, fingerprint gates — which checks are the actual boundary vs accreted ceremony; any remaining hand-frozen "expected value" constants of the retired `CODEX_FEED_BASELINE` class (it was defused as a blocker but the const remains — are there siblings still blocking?). X-B owns network-level atomicity; you own the authorization/validation logic.

**Data layer — `CONC_Recipe_Data.js` (11,983 lines): NEVER read attentively.** Node scripts only (write scripts to scratchpad):
- Schema uniformity across records: key-set variance, type variance per field (e.g. `allergens` array vs string vs missing; `instructions` string vs `{text}`).
- Duplicate recipe names/ids; orphan references both directions (names referenced by index.html tables/features that don't exist in data; data entries no surface can reach).
- The `allergens || []` flattening class (a KNOWN_VS_SHOWN finding: *never-verified* rendered as *analyzed-clean*) — check the current state of the feed writer and the in-app render: is "no allergen data" distinguishable from "verified none" today?
- `concRecipeLibrary` localStorage size pressure: measure seed size; estimate a realistic library at 240+ recipes with cookLogs — quota class (5–10MB shared origin).

**Also:** boot migrations in range (seed-merge S4 polarity, `concSharedStatusMigration`, status folds) — order dependence + provenance-discriminator correctness (the "which paths actually write this field" lesson); the D4-removed wrappers (`_cxRecipeWorkflow*` etc.) — verify no dangling references (incl. string/onclick contexts).
