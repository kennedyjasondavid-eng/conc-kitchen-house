# Slice M-1 — MISE/CODEX front half (L3,633–18,141)

Read `_COMMON.md` first. Findings → `findings/M-1.md`. Repo: `/home/user/conc-recipe-hub` (record HEAD sha).

**App:** `index.html`, 36,482 lines — recipe library / MISE engine / CODEX explorer; upstream allergen + recipe feed for DOOR and EXPO (food-safety-relevant data source).

**Skim rule (run FIRST):** `git -C /home/user/conc-recipe-hub log --since=2026-08-09 --stat` — hunks merged in the Aug 10–12 sweeps (PR #78 UX-friction, recipe-flow S0–S6, #81) are skim-class: freshly reviewed, lowest yield. Note which of your regions those diffs touched.

**Attentive range: L3,633–18,141** — navigation (@4085) → library/browse/detail surfaces, order list (@14488), XLSX/SharePoint export (@14885), translator (@15593). CSS L1–2555 + HTML shell L2555–3633: sampled dead-selector/dead-id checks only.

**Leads to deepen:**
- **Escaping coverage at sinks.** The file has ~172 `innerHTML =` sinks; the 2026-06-13 XSS pass fixed ~57. The question is not "was the pass good" but **are sinks ADDED SINCE June escaped** — new feature surfaces since then: costing workbench, release queue, home-site tag chips, "Add to the collection" act, digest list. For any sink rendering recipe-authored text (names, ingredients, notes, tags — all cross machine boundaries via import/share), verify the escaper AND its context (`_esc` vs `_escJsAttr` inside inline handlers — context mismatch is the documented class).
- **Export paths stay read-only** (the salt-bug provenance anti-pattern): verify XLSX/translator/print exports never mutate the stored recipe (no ensureX-style writes on read paths added since).
- `api.anthropic.com` calls @16714 (in your range boundary; also @22761 in M-2): what is this feature, where does the API key live (localStorage? hardcoded?), what happens on failure, and does its response text reach an HTML sink unescaped? Describe mechanism precisely; X-B owns network-failure depth.
- Duplicate/near-twin render helpers between library cards, site cards, review queue (copy-paste divergence class).

**Boundary note:** back half (settings, Graph publish, wizard, unbannered tail) = M-2. Data-layer script checks = M-2. Cross-app storage keys = X-C.
