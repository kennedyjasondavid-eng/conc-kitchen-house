# Slice X-C — shared-origin storage + duplicated vocabulary

Read `_COMMON.md` first. Findings → `findings/X-C.md`. Record HEAD shas of all repos touched.

All six apps serve from ONE GitHub Pages origin → one localStorage namespace. A prior P0 lived exactly here (EXPO's "Clear all safe keys" deleting DOOR/MISE/HUB state + the shared token — since fixed with owner-guards). Your job: the definitive key map + collision/clobber hunt + the vocabulary-duplication grading.

1. **Full key census** (the deliverable table): for each app, every `localStorage`/`sessionStorage` key — literal AND **constructed** (find the constructor fns: EXPO `_orderSheetBucketKey`/`_orderSheetOnHandKey`, HUB `source.cacheKey` per-source, PROOF `SOURCES[].cache`, MISE const-named keys like `CONC_COST_OVERRIDES`). Output: key/family → {app: R/W/remove} matrix.
2. **Confirm the 5 known collisions at file:line and trace consequence:**
   - `concRecipeLibrary` — MISE writes (~24460, declared "cross-app contract → Production Hub"); HUB reads (~3086); EXPO's cleanup-gate protects it. Same shape expectations? Does HUB tolerate every shape MISE can write (incl. mid-migration)?
   - `conc_gh_token` — DOOR (~11533) / EXPO (~22347) / HUB (~1080): one token, three writers — sanitization/validation differences (EXPO strips non-ISO-8859-1; do the others? can DOOR save a token EXPO then chokes on, or vice versa?); plus the four *per-app* token keys duplicating purpose (`pp_gh_token` legacy, `hub_gh_token`, `conc-door-gh-token-meta`, `conc_hub_gh_settings` in MISE) — which are live vs vestigial; single-source-of-truth grading.
   - `concRegistryState` — DOOR writes (~9784); who else reads/clears?
   - `concRecipeSettings` — HUB (~3869) + MISE (~20726): same schema? writer wars (one app's save clobbering the other's fields)?
   - `hub_schedule_cache` — HUB writes (~4282); EXPO's relationship to it (test-only or app code?).
3. **Hunt UNKNOWN collisions/clobbers:** every app's clear/reset/cleanup/migration path (DOOR boot clears + retired-state clears; EXPO cleanup panel + `ensureStorageHeadroom` + boot migrations; MISE migrations + any settings reset; HUB cache clears; PROOF `prefSet` whitelist refusal) checked against the full matrix — can ANY app delete/overwrite a key another app owns today? Also prefix-family gaps: keys with NO app prefix (collision-prone by construction).
4. **Quota pressure:** biggest stored values per app (measure seeds where possible: `pp_canonical_save` ~1.1MB, `concRecipeLibrary` seed, caches `pp_door_menu_cache`/`hub_schedule_cache`/`proof_cache_*`); realistic combined profile on one shared origin vs the 5–10MB quota; who fails first and HOW (which apps pre-check with headroom logic — EXPO does, who else? — vs raw setItem throw; is the failure loud?). Cross-app quota theft: one app's bloat breaking another's save.
5. **Cross-tab:** any `storage` event listeners anywhere? (Two tabs of one app = documented writer hazard; note current guards or their absence, per app.)
6. **Vocabulary duplication (grade vs data-over-code):** exact diffs of — the 3 site vocabularies (`Bloor/Rex/LAN` EXPO+HUB · `Bloor/Rexdale/Lansdowne` DOOR + HUB `KNOWN_SITES` (+GCNC) · lowercase `bloor/rexdale/lansdowne` MISE + EXPO site_profile json) + HUB's 5 inline lists + MISE's ~10 label maps; WHERE does a mismatch have live effect (any seam that string-matches site names across apps or across tables?). Also: the cycle-anchor duplication chain (EXPO const → `_cycleStart` string → HUB/PROOF parses → PROOF's divergent SAMPLE '2026-05-10') and the 4 versioning conventions — as duplication/consistency findings (X-B owns their order-compare risk; P-1 owns TZ).
