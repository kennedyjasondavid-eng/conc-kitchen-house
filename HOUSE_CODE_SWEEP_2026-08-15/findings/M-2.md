# Slice M-2 — MISE/CODEX back half (L18,141–36,482) + data layer by script

- Sweep date: 2026-08-15 (session continues into 2026-08-16 UTC)
- Repo: `/home/user/conc-recipe-hub`
- HEAD at sweep start: `6ea8b30` "Cheat sheet: add dish→EXPO-routing bridge (§7b) + two routing gotchas (#81)"
- File sizes: `index.html` 36,482 lines; `CONC_Recipe_Data.js` 11,983 lines (data layer, script-only per brief — never read attentively)
- Mode: TOKEN-LEAN (fresh restart; prior run for this slice died before writing anything)

## Coverage statement

STATUS: COMPLETE for this session's token-lean budget. Six findings (2×P1, 4×P2) written, each with verified evidence at HEAD `6ea8b30`. Full detail — exactly which line ranges were Read-tool-attentive vs. `sed`-pulled vs. grep-only vs. entirely unswept — is in the **Limitations** section below (written per the TOKEN-LEAN MODE honesty requirement: an unread region is stated as unread, never implied swept). Short version: attentive coverage concentrated on the brief's stated priorities — the publish-authority/identity-map/release-queue tail (`L30218-31919`, `L32150-32749`), SharePoint+Graph (`L24240-24457`), AI/M365 settings (`L22640-22772`), site-profiles intro (`L20466-20595`), the variant wizard (`L26684-27060`), and the rename/destination-receipt/seed-merge boot-migration block (`L24595-25669`) — plus the `CONC_Recipe_Data.js` data layer via Node scripts only (never read attentively, per instruction). The largest genuinely-unswept stretches are `L20600-24240` (bulk of Settings: cost workbench, Curator Tools, Recipe Index, Library ingest, batch export, Diagnostics, import/export) and `L27061-30218` + `L32874-36482` (front half and back half of the unbannered tail beyond the publish/identity core) — both grep-mapped by function name but not read.

---

## Skim-rule check (run first)

`git log --since=2026-08-09 --stat --oneline`: confirms the Aug 10–12 line — recipe-flow simplification S0–S6 (PR #77) + UX-friction remediation Lines A/C/D/B (PR #78) + cheat-sheet doc (#80/#81) — is skim-class and matches KNOWN.md verbatim ("MISE Aug 10-12... merged; cheat-sheet #81"). Treated as reviewed; not re-litigated below except where a finding falls directly in a touched region and adds new evidence.

## Slice map (banners, L18,141+)

```
18141 ADVANCED MODE
20385 ENGINE VALIDATION
20466 SETTINGS MANAGEMENT / SITE PROFILES  (cost workbench, curator tools, recipe index, M365/AI settings, library batch tools, diagnostics)
22768/22770/23182  (unlabeled === dividers inside Settings)
24240 SHAREPOINT + GRAPH
24457 RECIPE LOG  (actually: rename engine, fingerprint/destination-receipt machinery, saveToRecipeLibrary, seed-merge/status migrations, landing search, toasts, THEN the literal cook-log feature ~26343+)
26482 NOTES FIELD HELPERS / END / MY RECIPES TAB SWITCHER / ARCHIVE BROWSER
26684 CREATE VARIANT WIZARD
27061 END VARIANT WIZARD  ← last banner in the file
27061–36482  UNBANNERED TAIL (326 top-level fns): browse library, workspace/catalog tabs, collection/nomination, review panel, identity/fingerprint helpers (_cx* family), handoff package + shared-review-transport envelopes, publish-authority classification, Library Release Queue, DOOR projection/preview, identity map + authority guard, publish dry-run + sidecar metadata + actual publish, promote/depromote, recipe-view/rename/edit, scaling/procurement projections, XLSX translator, theme/wizard/codex-explorer chrome.
```

## Grep battery (whole index.html, counts noted; M-2-range hits called out)

- Empty catches `catch\s*\([^)]*\)\s*\{\s*\}`: whole file — many; in M-2 range (≥18141) present but not yet individually triaged beyond the publish/identity path (see M-2-2 below, none found wrapping a load-bearing write in the ranges actually read).
- `innerHTML=` / `insertAdjacentHTML` / `document.write` / `outerHTML=`: present throughout M-2 range (render functions for panels/tables). Not individually triaged for XSS in this pass — CLAUDE.md records a dedicated app-wide XSS hardening pass (2026-06-13, ~57 sinks fixed via `_esc`/`_escJsAttr`) predating everything I read; the sinks I did read in passing (`renderPublishDryRun`, `updateVariantName`/`openVariantWizard`, the M365/AI settings tabs) consistently used `_esc()` on dynamic text. Did not specifically audit the newer handoff-package/shared-review-transport preview renderers (`L27061-30218`, unread — see Limitations) for the same discipline; flagging as a follow-up check rather than a finding since I have no positive evidence of a gap there, only that I didn't look.
- `localStorage.(setItem|getItem|removeItem)` in M-2 range: numerous (site/AI/M365 settings, seed-merge receipts, recipe library, recipe log, GH publish token). No constructed/templated key names found in range (`grep` for `+`/backtick concatenation into a storage call returned empty) — all keys are static string literals, good practice (rules out a whole class of storage-key drift).
- Version order-compares: no numeric version `<`/`>` comparisons found in range. `localeCompare` used 13× in range — all for display sort (recipe names, tags), not version/identity comparison; benign.
- Bare `JSON.parse` in range: 38 occurrences — spot-checked the seed-merge/identity ones (wrapped in try/catch at their call sites per existing convention; see M-2 write-up below).
- Inline `on\w+="` handlers in range: 247. Not exhaustively set-diffed against defined functions (budget); the ones encountered while reading the variant wizard (`selectTwistType`/`selectTwistValue` — `opt.replace(/'/g, '&#39;')` on button-attribute values) and elsewhere were consistently escaped.
- `setTimeout(` in range: 21 — toast dismissal timers and one deferred focus call; none used as a cross-write ordering hack in the areas read.
- `new Date(` with a string argument in range: 0 matches.
- **Duplicate top-level `function <name>` declarations (whole file): `renumberSteps` (2×) and `metric` (2×).** `metric` is a nested closure-scoped helper independently defined inside two different outer functions (`buildCostWorkbenchResultsMarkup` @21079 and `buildCuratorQualitySummary` @21416) — harmless, separate scopes. **`renumberSteps` is a REAL top-level collision — see M-2-1.**

## Findings

### M-2-1 · Duplicate top-level `renumberSteps` declaration silently breaks step renumbering in the from-scratch/paste recipe builder
- class: kludge
- severity: P2
- status: NEW
- confidence: verified (traced both declarations, both call sites, and the confirmed-safe data-harvest path at HEAD)
- where: `index.html:20297` (`function renumberSteps(container) { const rows = container.querySelectorAll('.instruction-row'); ... }`) vs `index.html:33989` (`function renumberSteps() { var rows = document.querySelectorAll('#methEditRows .step-edit-row'); ... }`) — both are plain top-level `function` statements in the single shared script scope (confirmed: the only functions declared at a shallower indent than the rest of the file are the 13 Cuisine-Codex-sidebar helpers at L36223+, a genuinely separate small scope; both `renumberSteps` defs sit at the same 4-space indent as everything else, i.e. one shared namespace).
- what: Two unrelated features (the manual/"From Scratch"+Paste recipe **builder**'s instruction-step reorder/remove, vs. the browse-view recipe **editor**'s method-step editor) each declare their own `renumberSteps`. Because both are plain function declarations in one scope, the later one in source order (L33989, no-arg, queries `#methEditRows .step-edit-row`) silently wins for **every** call in the file, including the two calls at L20266 (`moveStep`) and L20279 (`removeStep`) that pass a `container` argument the winning definition doesn't even have a parameter for.
- evidence: callers `renumberSteps(container);` at L20275 and L20286 (inside `moveStep`/`removeStep`, operating on `#manualInstructionSteps` rows); the surviving definition body is `document.querySelectorAll('#methEditRows .step-edit-row')` — an element ID that only exists in the (unrelated) recipe-edit view rendered by `toggleRecipeEdit`. When called from the builder, that selector matches 0 elements, so the loop body never runs.
- consequence: In the manual/blank recipe-builder canvas (the D2-simplified "Blank tab lands directly in Build" path, per Recent §Aug-10-11), reordering or deleting an instruction step never updates the visible `1./2./3.` labels next to the remaining textareas — they go stale/out of order on screen. **Not a data-loss bug**: verified `collectRecipeFromCanvas` (L20337-20345) derives the saved `instructions[].number` from live DOM order (`stepDivs.forEach((div, idx) => ... number: idx+1`), never from the stale `.instruction-number` text, so the persisted/finalized recipe's step order and numbering are correct regardless. The user-facing harm is confusion while authoring a multi-step method (which visible "step 3" you're editing may not match its saved position) — a Known≠shown surface defect (lens 3), not a food-safety or persistence defect. The recipe-EDIT view's own `renumberSteps()` (L33989) is unaffected — it's the one that actually runs, correctly, for its own feature.
- direction: Rename one of the two (e.g. the builder-canvas one back to its own name, matching the existing project convention of prefixed helpers like `_cxRerenderIngTable`/`_ingEditRow` used elsewhere in the same edit-row family) — a same-file, no-architecture-change fix consistent with the existing naming seam already used for the sibling edit-row helpers at L33920-33994.

---

### M-2-2 · The "Publish Now" button is permanently, unconditionally blocked by a hardcoded constant — ~300 lines of approval-guard ceremony gate a boundary that reduces to one `false`
- class: kludge
- severity: P1
- status: NEW
- confidence: verified (traced the full call chain from the UI button through the boundary constant at HEAD; confirmed the constant has exactly one assignment in the whole file and nothing ever flips it)
- where: `index.html:22994` (the "Publish Now" button, `buildPublishTab`) → `publishRecipeProduction` `:32656-32667` → `buildPublishDryRun` `:32294-32295` (merges `doorPublishGuard.blockers` into the top-level `blockers` that gate the early-return) → `buildDoorPublishGuard`/`_buildDoorPublishGuardWithApproval` `:31229-31231,31133-31227` → `_cxDoorLivePublishApprovalStatus` `:31042-31073` → `DOOR_LIVE_PUBLISH_APPROVAL` `:30569-30583`.
- what: Settings → "Publish to GitHub" renders a primary-styled (`background:var(--primary);color:white`) **Publish Now** button next to Test Connection / Dry Run, described as pushing `recipe_production.json`/`DOOR_RECIPE_DATA.json` "so the Production Planner can fetch it on load." `publishRecipeProduction()` hard-returns before any `ghPushFile` call whenever `buildPublishDryRun().blockers.length > 0` (`:32659-32667`). That array always includes `doorPublishGuard.blockers`, which always includes `_cxDoorLivePublishApprovalStatus`'s blocker `'DOOR publish guard: broad in-app Publish Now approval for the projected DOOR feed is not recorded...'` because the gating test is `liveApproval.approvalStatus === 'approved' && liveApproval.approved === true` (`:31045`) against `DOOR_LIVE_PUBLISH_APPROVAL = { approvalStatus: 'pending', approved: false, ... }` — a plain object literal declared once, never reassigned anywhere in the file (verified: `DOOR_LIVE_PUBLISH_APPROVAL\s*=` matches only its own declaration).
- evidence: `if (dryRun.blockers.length) { renderPublishDryRun(); ...; return; }` (`:32659-32667`, no `ghPushFile` reached); `DOOR_LIVE_PUBLISH_APPROVAL`'s own inline note says it plainly: `'Broad in-app Publish Now remains blocked pending separate publish-path approval, authority reporting, and identity readiness.'` (`:30581`). Getting there costs ~300 lines of machinery: three nested per-field approval-object comparisons (`_cxDoorProjectionApprovalStatus` 8 hardcoded commit-hash/doc-path checks, `_cxDoorContractBaselineApprovalStatus` 10 more hardcoded version/hash/path checks) that all compute and report `blockers`/`readyFor*` flags, none of which affects the outcome — the button is unreachable regardless of whether every other check passes, because this one boolean alone decides it.
- consequence: A curator who fills in a token+repo and clicks the prominent, correctly-labelled "Publish Now" button will *always* see "Publish blocked - review dry run first" / "Publish blocked - resolve dry-run blockers first," regardless of library state — even a single, fully seed-matched, zero-defect canonical recipe. Nothing in the UI marks the button as structurally disabled or explains that it is closed by policy, not by a fixable data issue; the only signal is a small `#hub-gh-status` text line under the button after the fact. This confirms (independently, from source) what the checked-in feed history already implies: `recipe_production.json`/`DOOR_RECIPE_DATA.json` updates are landing through an out-of-band process (architect-authored PRs), never through this in-app control — so the control is dead weight that can mislead a session into (a) burning time trying to satisfy dry-run blockers that can never matter, or (b) concluding the per-record authority gate is what's protecting the feed, when the actual, sole, load-bearing boundary today is this one hardcoded `false`. This is exactly the lens-9 "ceremony ≠ boundary" pattern the S6/C4 fix already had to defuse once for `CODEX_FEED_BASELINE`; this is a live, undefused sibling one layer deeper.
- direction: Either wire "Publish Now" to reflect this reality (disable/relabel the button and surface the real reason up front, the same honesty move C4 made for the record-count cry-wolf — "point the button's enabled-state and copy at the real gate, not a diagnostic panel buried below it"), or, if a working in-browser publish path is wanted, make `DOOR_LIVE_PUBLISH_APPROVAL` a real Jason-toggle (the same pattern already used for the *other* approvals in this file that DO get updated, e.g. `CODEX_IDENTITY_MAP_AUTHORITY_APPROVAL`'s dated re-approvals) instead of a silent dead end.

### M-2-3 · `CODEX_FEED_BASELINE` was defused as *one* cry-wolf blocker; several structurally identical hand-frozen record-count siblings remain live comparison operands
- class: kludge
- severity: P2
- status: NEW (extends KNOWN pattern — S6/C4 fix is documented in CLAUDE.md and KNOWN.md doesn't list this as still-open)
- confidence: verified (read all four approval-object literals and the one comparison the brief flagged; the sibling constants are visible in the same read)
- where: `index.html:28692-28698` (`CODEX_FEED_BASELINE`, still `doorRecordCount:131` — never bumped), compared at `:31009-31010` inside `_cxDoorProjectionApprovalStatus`; sibling hand-typed record-count fields at `:30518-30519` (`approvedScope.recordCountFrom:131, recordCountTo:240`), `:30555` (`DOOR_CONTRACT_BASELINE_APPROVAL.approvedProjectedRecordCount:240`), `:31246,31266` (`CODEX_IDENTITY_MAP_AUTHORITY_APPROVAL`/`CODEX_PRIMARY_FEED_IDENTITY_ARTIFACT_APPROVAL.approvedRecordCount:240`), `:31272-31273` (`approvedDoorRecipeIdCount:240`, `approvedRecipeProductionRecipeIdCount:240`).
- what: The Aug-11 S6/C4 fix (CLAUDE.md "Recent") deliberately deleted the *display* cry-wolf ("would change from frozen baseline") and repointed the *displayed* baseline to the live count, but its own comment (`:31167-31170`) says `CODEX_FEED_BASELINE` "is still read above for the approval-scope coherence check, which is untouched" — i.e. `CODEX_FEED_BASELINE.doorRecordCount` (frozen at 131 since 2026-06-17) is still a live blocker input, just one that currently happens to agree with `DOOR_PROJECTED_PUBLISH_APPROVAL.approvedScope.recordCountFrom` (also 131) so it stays silent. At least 6 more integer literals across 4 separate `var` object declarations (spread ~750 lines apart in the file) encode "how many records exist right now" and must be hand-kept in sync with each other and with the real library count on every future growth — exactly the maintenance pattern that already produced the 224-vs-240 drift S6 had to firefight once.
- evidence: `if (Number(scope.recordCountFrom) !== Number(baselineCount)) { approvalBlockers.push(...) }` (`:31009-31010`) where `baselineCount = CODEX_FEED_BASELINE.doorRecordCount` (`:31143`); `CODEX_FEED_BASELINE = { ...doorRecordCount: 131... }` unchanged since capture (`capturedAt: '2026-06-17'`).
- consequence: This particular check is silent today only because two independently hand-edited constants happen to still agree (131≈131); it is not derived from anything live. If the identity-map/contract-baseline approvals are ever re-ratified for a future recipe-count change (as they already were once, 225→240, per the `CODEX_IDENTITY_MAP_AUTHORITY_APPROVAL` re-approval note dated 2026-07-28) without also bumping `CODEX_FEED_BASELINE.doorRecordCount`, this specific blocker reactivates with the same cry-wolf character C4 just removed elsewhere — feeding into `doorPublishGuard.blockers`, which (per M-2-2) already renders the button permanently unusable for an unrelated reason, so the practical UI impact is currently nil, but the diagnostic panel would report a fresh, false "baseline mismatch" that could send a future session chasing a non-issue.
- direction: Since M-2-2 shows the whole `doorPublishGuard` apparatus is presently inert (gated shut by one separate constant regardless), the cheapest fix is deleting the `CODEX_FEED_BASELINE`-vs-`approvedScope.recordCountFrom` comparison outright (as C4 already did for the sibling display warning) rather than hand-maintaining yet another frozen number in lockstep with the others — same direction C4 already took, just not yet applied to this specific remaining comparison.

---

### M-2-4 · Both live Anthropic API calls hardcode the same dated model snapshot (`claude-sonnet-4-20250514`), and every non-OK response is reported to the user as a key problem
- class: kludge
- severity: P2
- status: NEW
- confidence: verified (the hardcoding, duplication, and error-message collapsing are read directly from source); plausible (that `claude-sonnet-4-20250514` is at increased risk of deprecation this far past its release — I cannot check Anthropic's live model-availability from this session)
- where: `index.html:22761` (`testApiKey`, the Settings → AI Features "Test" button) and `index.html:16717` (the real Smart-Mode translator call, inside the AI-parse path used when `ai.smartMode && ai.apiKey`).
- what: Both the connectivity-test call and the actual feature call independently hardcode `model:'claude-sonnet-4-20250514'` with no shared constant, no fallback, and no user-facing model selector. Both collapse every non-2xx response into a single generic message: `testApiKey` shows `'✗ API error ' + r.status + ' — check your key.'` for *any* failure (`:22763`), and the translator path does `if (!res.ok) throw new Error('API error ' + res.status)` (`:16718`), which (based on the surrounding pattern used elsewhere in this file for thrown errors) surfaces as a generic failure toast rather than distinguishing "your key is bad" from "the pinned model id is no longer valid."
- evidence: identical literal `model:'claude-sonnet-4-20250514'` at both call sites (only other occurrence in the file); `testApiKey`'s catch-all copy is literally "check your key" regardless of whether `r.status` is 401 (auth) or 404/400 (e.g. a retired/unknown model id).
- consequence: If this specific dated snapshot is ever retired by Anthropic (a routine occurrence for dated model ids over time), both AI-backed CODEX features fail simultaneously and a curator troubleshooting via the "Test" button is actively pointed at the wrong culprit ("check your key") when the key is fine and the model id is stale — a Known≠Shown defect (lens 3): the HTTP status the code already has in hand distinguishes the cases, but the message discards that distinction. Low blast radius (Smart Mode is an optional recipe-translation aid, not a food-safety or publish path), but the duplicated hardcoding means a future fix requires remembering to update it in two places.
- direction: One shared `const CODEX_AI_MODEL = 'claude-sonnet-4-20250514'` referenced by both call sites (matches this file's existing single-source-of-truth conventions, e.g. `DOOR_PROJECTED_CONTRACT_VERSION`/`CODEX_IDENTITY_MAP_VERSION`), and branch the error copy on `r.status` (401/403 → key problem; other 4xx → "the AI service rejected the request" rather than blaming the key) — mirrors the honest-status-based branching this file already does elsewhere (e.g. `testHubGHPublish`'s 404-vs-401-vs-other handling at `:32724-32726`, a few hundred lines away, which is exactly the pattern this AI path lacks).

---

### M-2-5 · `allergens: []` is structurally indistinguishable from "never verified" for at least 12 of 240 production recipes, even though the display layer has a 3-state model built to make exactly that distinction
- class: data
- severity: P1
- status: NEW
- confidence: verified (read the display function, then measured the actual seed data with a script; the reachability gap is a direct logical consequence of the two, not an inference)
- where: `index.html:14250-14255` (`allergenSummary(a)`) vs the measured shape of `CONC_PRODUCTION_SEED` in `CONC_Recipe_Data.js` (240 records, script-verified: `missing=0, null=0, emptyArray=12, nonEmptyArray=228, nonArrayType=0` — every single record's `allergens` field is a real array, never `null`/absent).
- what: `allergenSummary(a)` is a genuine 3-state helper — `arr && arr.length` → `'contains'`; `arr` (truthy, i.e. a real but *empty* array) → `'clean'`, text *"No allergens detected — verify before service"*; `arr == null` (missing/undefined/null) → `'unverified'`, text *"Not verified — check before service"*. This is exactly the right shape to separate "we checked, found nothing" from "nobody has looked yet" — **but the distinction only works if the underlying data actually uses `null` for the unchecked case.** It never does in the current library: every one of the 240 `CONC_PRODUCTION_SEED` records already carries `allergens` as an array (script-confirmed: 0 missing, 0 null). So the `'unverified'` branch is unreachable for any seeded production recipe today, and 12 of them have `allergens: []` — Apple Crisp Dessert, Apple Crisp, Garlic Sauce, Shawarma Vinaigrette, Roasted Carrot Soup, Plain Rice — Regular oven method (Rexdale), Plain Rice (Rexdale), Tumeric Quinoa, Turmeric and Pea's Rice, Johnny Cakes (2), +2 more.
- evidence: script output `allergens field distribution: missing=0 null=0 emptyArray=12 nonEmptyArray=228 nonArrayType=0`; `allergenSummary` source at `:14250-14255` showing the `arr == null` branch is the only path to `'unverified'`.
- consequence: A curator or DOOR-side consumer looking at any of these 12 recipes sees the confident-sounding "No allergens detected" state, structurally identical to a recipe whose allergens were genuinely scanned and came up empty (e.g. plausible for Plain Rice) versus one where the field simply was never populated (plausible for a condiment/sauce like Garlic Sauce or Shawarma Vinaigrette, which get folded into other dishes and are exactly the class of item easy to skip during authoring). Allergen data is the safety-critical input to DOOR's anaphylactic/allergen routing (pipeline: MISE/CODEX → DOOR/EXPO); this is the CODEX-side root of the class DOOR's own CLAUDE.md repeatedly treats as a live hazard (the Egg-Salad-Wrap stray `hasPork` incident, the 2026-07-19 pork-halal guard). This is a Known≠Shown defect (lens 3) at the data layer, not the UI layer: the render code already knows how to say "not verified," it just never receives the signal that would let it.
- direction: `isAllergenPlaceholder` (`:14259-14265`, right next to `allergenSummary`) already treats specific placeholder STRINGS as re-import guards — the same file already has the vocabulary and the seam for "mark this field's provenance honestly." Extending that convention to the write side (e.g., an explicit `allergensVerified:boolean` companion field set only by `detectAllergens()`/curator sign-off, defaulting `false` on seed-import so `allergenSummary` can check it instead of `== null`) would let the existing 3-state display logic actually reach `'unverified'` for the seed's true unknowns instead of silently reporting them as scanned-clean.

### M-2-6 · The checked-in `CONC_PRODUCTION_SEED` literal disagrees with the checked-in `recipe_production.json` on where 93/240 recipes' production facts live — the feed-writer only reads one of the two shapes, so a fresh-device reseed would silently zero them
- class: data
- severity: P2
- status: NEW
- confidence: verified (schema split measured directly; confirmed the feed-writer's exclusive read path in source; confirmed the *currently checked-in* `recipe_production.json` is unaffected today, so this is a latent/fresh-boot risk, not a live defect — self-checked before writing this up)
- where: `CONC_Recipe_Data.js` (`CONC_PRODUCTION_SEED`, 240 records) vs `index.html:28616-28667` (`buildRecipeProductionJSON`, the feed-writer).
- what: Script-measured: within `CONC_PRODUCTION_SEED`, `equipment`/`primaryTemp`/`sourceSite` (top-level fields) and `production` (a nested object carrying `cookMin`/`ovenMin`/`stoveMin`/`prepMin`/`equipment`/`cookSite`/`packaging`/etc.) are **100% mutually exclusive across all 240 records** — 93 records have the top-level triple with `production:null`; 147 have `production` populated with no top-level triple; zero records have both, zero have neither. `buildRecipeProductionJSON` (the function that emits `recipe_production.json`) reads exclusively `var p = entry.production || {}` and then `p.cookMin||0`, `p.equipment||'none'`, `p.cookSite||'Bloor'`, etc. — it never reads the top-level `equipment`/`primaryTemp`/`sourceSite` fields at all.
- evidence: correlation script: `hasTopLevelEquipment & hasProductionObj: 0`, `hasTopLevelEquipment & NO productionObj: 93`, `NO topLevelEquipment & hasProductionObj: 147`, `NO topLevelEquipment & NO productionObj: 0`. Cross-checked against the actually-checked-in `recipe_production.json`: **the live published feed is fine today** — e.g. "Apple Cake" (one of the 93 top-level-shape seed records) publishes with real `cookMin:35, ovenMin:35, equipment:"oven", cookSite:"Rex"` — because `buildRecipeProductionJSON()` reads from the *live, in-browser, localStorage-merged* library (which has since had `production.*` filled in via the UI, e.g. `saveProductionData` in Settings→Costs), not from the static `CONC_PRODUCTION_SEED` literal directly. Of the 93 seed records checked against the published feed, 0/93 show the zero-cook/no-equipment pattern the seed literal alone would produce.
- consequence: The checked-in seed file and the checked-in feed file, sitting in the same commit, encode the same 93 recipes' production facts in two different, non-interoperable shapes — one of which (`equipment`/`primaryTemp`/`sourceSite`) the feed-writer cannot read at all. `initProductionLibrary()`'s seed-install branch for a genuinely new record (`if (!e) { lib[entry.recipeName] = entry; ...}`, `:25511-25514`) installs the raw seed object verbatim, and its production-merge branch (`if (entry.production && (!e.production...` `:25564`) only fires when the *seed's* `production` is truthy — never for these 93. So on a brand-new device/browser (empty `concRecipeLibrary`, e.g. a second kitchen laptop, or anyone using DOOR/EXPO's "Clear all data" class of storage-cleanup action on this shared origin), these 93 recipes would seed with `production:null`, and the very next `buildPublishDryRun()`/`renderPublishDryRun()` ("Dry Run") would report `cookMin:0, equipment:'none', cookSite:'Bloor'` for all of them until someone manually re-enters the same production facts a prior session already worked out — a real but *latent* regression, not a currently-live one.
- direction: Fold the top-level `equipment`/`primaryTemp`/`sourceSite` triple into `production.*` the next time `CONC_PRODUCTION_SEED` is re-baked from the live library (the same "seed release" ceremony already described in this file's Rules section — "Seed recipes are architect-controlled... periodically re-baked"), so the checked-in seed and the checked-in feed agree on one shape before the next fresh-device boot needs it.

---

## Confirmed-healthy

Things checked expecting problems, found solid:

- **D4 dead-code sweep (Aug-12) left zero dangling references.** All 8 functions CLAUDE.md names as deleted (`copyAllergenList`, `printRecipeCard`, `openSettingsTool`, `copySubmissionJSON`, `variantFromLibrary`, `toggleCompPopover`, `batchExportAllRecipes`, `renderFlavorProfiles`) return **0** occurrences anywhere in `index.html`, including string/`onclick` contexts (word-boundary grep over the whole file, not just function-definition sites). `_cxRecipeWorkflowState` (the live core function, distinct from the retired thin `_cxRecipeWorkflow*` wrappers) is intact and has 7+ real call sites.
- **Seed-merge / boot-migration order-dependence (S4 polarity + `_cxMigrateSharedStatus`) does not actually have the footgun the brief asked me to hunt for.** `initProductionLibrary()` runs *before* `migrateLegacyLibraryEntries()` at all 4 boot call sites, consistently (never the reverse at any site) — and traced through `_hasLocalContentProvenance`'s discriminators (`contentFingerprint`, `production._lastEdited`, `revisionProposal`, `publishAuthority`, destination receipts, non-`'created'` history), none of them actually depend on the legacy-default fields `migrateLegacyLibraryEntries` back-fills (`status`/`producedAt`/`servedAt`/`tags`/`promotedBy`/`history`/`cookLog`) in a way that changes the provenance verdict — a record with no `history` field at all reads identically to one with only `[{action:'created'}]`. The provenance-discriminator design is sound against this specific order.
- **`renameRecipe` and the S3b destination-receipt/fingerprint machinery** (`_cxRecipeContentFingerprint`, `_cxResetDestinationAuthorityForContentChange`, `_cxRecordDestinationTransition`, `_cxEditedSinceReceipt`) read as carefully reasoned, self-documenting code with explicit review-fold comments (e.g. "Review fold P2-1/P2-2/P3-1/P3-3") tied to specific proven failure modes (case-only rename resurrection, fingerprint-vs-receipt lockstep, cross-dish id collision via foreign-proposal rewrite). No new defect found on a direct read.
- **The Anthropic-key storage mechanism is unremarkable** — `currentSettings.ai.apiKey` persists via the same `saveSettings()` → `localStorage.setItem('concRecipeSettings', ...)` path every other setting uses; no bespoke/weaker handling. Browser-side exposure of the key is inherent to the whole HOUSE architecture (single-file, no server, no build) and already the subject of dedicated cross-app work elsewhere (EXPO's save-trust `conc_gh_token` line) — not a CODEX-specific gap.
- **No constructed/templated `localStorage` key names** anywhere in the M-2 range (`≥L18141`) — every storage call uses a static string literal. Rules out a whole class of storage-key drift the grep battery is designed to catch.
- **`recipe_production.json` / `DOOR_RECIPE_DATA.json` publish payload shape is unchanged by any finding here** — M-2-2/M-2-3 are about the *gate* around publishing, not the payload; M-2-6 is confirmed NOT currently manifesting in the live checked-in feed (see that finding's evidence).

## Limitations

- **Read-tool budget spent (~2,900 of the ~3,500-line cap):** attentively read via the Read tool: `L30218-30474` (publish-authority classification, Library Release Queue), `L30980-31284` (DOOR publish-guard approval chain), `L31615-31919` (identity-map authority guard + `buildRecipeIdentityMap`), `L32150-32349` (`buildPublishDryRun`), `L32547-32749` (dry-run render, `publishRecipeProduction`, `testHubGHPublish`, GH-settings save/load), `L24240-24457` (SharePoint+Graph), `L22640-22772` (M365 + AI settings tabs), `L20466-20595` (site-profiles intro), `L26684-27060` (variant wizard), `L24595-24977` (rename engine + destination-receipt machinery), `L25370-25669` (boot migrations: legacy-entry defaults, S2a shared-status fold, S4 seed-merge polarity). Additional small neighborhoods pulled via `sed`/sub-shell (not counted against the Read-tool budget but read into context all the same): `L28616-28667` (`buildRecipeProductionJSON`), `L28692-28710` (`CODEX_FEED_BASELINE`), `L30510-30590` (the three DOOR approval-object literals), `L22977-23014` (Publish tab UI + Diagnostics tab header), `L16695-16730` (Smart-Mode AI translator call), `L14250-14280` (`allergenSummary`/`isAllergenPlaceholder`), `L20297-20345` + `L33920-34010` (the `renumberSteps` collision + `collectRecipeFromCanvas`).
- **NOT attentively read** (grep-covered only, or entirely unswept): `L20600-22640` (bulk of Settings — cost workbench UI, Curator Tools, Recipe Index tab, Library ingest tool, batch export) — this is the largest unread stretch in my slice and is exactly where the June-July "Costing workbench / C6a-C7b" line lives; I relied on CLAUDE.md's own detailed receipts for that line rather than re-verifying it, since KNOWN.md/CLAUDE.md mark it merged+verified and my brief's explicit priorities (tail, SharePoint/Graph, AI settings, site profiles, publish ceremony, boot migrations) pointed elsewhere. `L23014-24240` (Diagnostics tab body/`runDiagnostics`, `saveSettings`/`exportSettings`/`importSettings`, `showBatchExportMenu`/`batchExportFiltered`, ingredient-sidebar macro logic) — not read; `exportSettings`/`importSettings` in particular is a plausible site for the load-bearing-write trace (user-data import/export) I did not get to. `L25670-26684` (rest of Recipe Log: `renderRecipeLog`/`saveRecipeToLog`/log CRUD, landing-search machinery, toast helpers, `renderRecipeLibrary`) — grep-covered only. `L27061-30218` (the front half of the unbannered tail: browse/workspace/catalog tab renderers, collection/nomination acts, review-panel machinery, handoff-package + shared-review-transport envelope builders, `_cxIdentitySiteScope`→`_cxIdentityDiagnostics`/`_cxLocalIdentity*` helpers at `L31283-31615`) — function-name-mapped via grep but not read; this is where I'd look first in a follow-up pass, particularly the handoff/shared-review-transport envelope validation (`_cxValidateHandoffPackage`, `_cxValidateSharedReviewTransportEnvelope`) given it's a genuine cross-device data-intake surface. `L32874-36482` (promote/depromote, recipe-view/edit/rename UI beyond what's noted above, scaling/procurement projections, the XLSX translator parser body, theme/wizard chrome, Cuisine Codex explorer) — grep-mapped (function inventory only), not read.
- **`buildPublishDryRun`'s per-record classification (`_cxClassifyPublishAuthority`) and the identity-map builder were read and traced, but I did not independently re-verify their arithmetic against a live populated library** (no browser/Playwright available in this session) — my CONC_PRODUCTION_SEED-vs-published-feed cross-check (M-2-6) used the actual checked-in JSON files as ground truth, which is a real but partial substitute.
- **Doc-drift spot-check (protocol step 5) was done opportunistically, not systematically** given the token-lean budget: caught one drift (this repo's own `CLAUDE.md` "What This Is" section says `recipe_production.json` "has no ingredient or cost fields," but the live `buildRecipeProductionJSON()` and the actual checked-in file both carry `ingredients`/`costPerPortion`/`costTotal`/`costCoverage` — low-severity, doc-only, not written up as a numbered finding to preserve the "10-25 substantial findings" bar, but noted here per the protocol's honesty requirement). Did not attempt a full 3-5-claim systematic pass beyond that.
- **CONC_Recipe_Data.js** was analyzed exclusively via a Node `vm`-sandboxed script (never read attentively, per the brief) against the file's `CONC_PRODUCTION_SEED` and `PROMPT_TEMPLATES` tables; the other ~20 data tables (`ALLERGEN_DB`, `NUTRITION_DB`, `COST_DB`, `CUISINE_CODEX`, etc.) were sized/enumerated but not schema-profiled in the same depth — a follow-up could extend the same script to those tables cheaply if warranted.
- **No live/browser verification** of any finding (no network egress to the deployed Pages site attempted; not required by the brief, and the in-flight collision guard in KNOWN.md means HEAD may have moved since `6ea8b30`).
