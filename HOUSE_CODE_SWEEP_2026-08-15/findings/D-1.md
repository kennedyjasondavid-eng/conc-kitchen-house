# Findings — Slice D-1 (DOOR full app + gate-coverage map)

- Sweep date: 2026-08-15
- Repo: /home/user/conc-kitchen-door — HEAD `7500521` "Merge pull request #74 from kennedyjasondavid-eng/claude/door-menu-footguns-design-bnz1ws"
- Files owned: `index.html` (21,553 lines), `tests/door-smoke.mjs` (2,289 lines, map-only)
- Coverage statement: IN PROGRESS — will be finalized at end of run.
- Pre-reads completed: `DOOR_POST_DEFUSE_REMEDIATION_HANDOFF_2026-08-15.md`, `KNOWN_VS_SHOWN_FINDINGS.md`, KNOWN.md DOOR section, `git log --since=2026-08-09 --stat` (defuse-merge hunks = skim-class).

## Grep-battery results

- Empty catches: **34** (`catch\s*\([^)]*\)\s*\{\s*\}`). Load-bearing candidates inspected below: `:11599/:11607` (`conc-door-anaph` write), `:17589/:17601` (special_meals/meal_swaps sidePublish), `:19424` (`concIntakeQueue` write), `:7708` (`concRecentLog`), `:9779` (provenance), `:12150/:12178` (SHA prefetch — KNOWN D1 territory).
- innerHTML sinks: **160**; `insertAdjacentHTML` 0; `document.write` **4** (`:10670,:10762` compliance print windows; `:21295,:21314` print/export windows — all template-literal, spot-checked below); `outerHTML=` 0.
- Bare `JSON.parse` (no `try` on same line): 33 raw hits — triaged below (most are inside try blocks on adjacent lines; real bare ones inspected).
- setTimeout: 27 — sequencing-suspects: `:19209` (`_ghHydrated=true` after 5s), `:19415` (`ensureRegistrySynced` 500ms), `:18343` (5s race timeout). Rest are UI-cosmetic timers.
- `new Date('string')`: 0 direct hits (string-arg date parsing checked separately — `parseISODate` exists).
- Duplicate `function` declarations: **1** — `showChangeLog` (KNOWN from KNOWN_VS_SHOWN_FINDINGS.md "Minor").
- Version order-compares: none on version identifiers (the `:19115` hit is a log line; comparison is `!==`). `localeCompare` 10 uses — all sort comparators, sound.
- Inline handlers: 114 distinct names; set-diff vs defined functions → **0 missing** (only parse artifacts). No dead inline handler targets.
- Self-confessed markers: 13 (`legacy` compat fields `:13596,:13607,:13649`, token migration `:11371,:11528`, xlsx ZIP workaround `:20449`, etc.) — triaged below.
- localStorage key census: ~35 distinct literal keys; no constructed-key writes found (all literals). Multi-key state clusters: registry (`concRegistryState`+`Snapshot`+`Provenance`), menu (`concUploadedMenu`+`Timestamp`+`concMenuBase`+`concMenuEdits`+`concMenuChangeLog`), NR-learning (`concLearnedNR`+`concNRResolutions`+`concUnresolvedNR`).

## Findings

### D-1-1 · Three CDN libraries un-pinned, un-guarded at most entry points — xlsx food-safety exports fail silently offline
- class: footgun
- severity: P1
- status: NEW
- confidence: verified
- where: index.html:689-691 (loads), :20439-20447 (`downloadSpecialFile`), :20105-20108 (`downloadPlatingSheet`), :21054 (`exportTestPlating`), :12596+ (6× `new Chart`), :20380/:20456 (JSZip)
- what: Chart.js, jszip, and xlsx-js-style load from two CDNs with **no `integrity=` SRI, no local fallback, no version-existence probe**. Only 2 of ~8 XLSX entry points are guarded (`exportComplianceXlsx :11163`, `handleImportFile :18328`). `downloadSpecialFile` — which builds the **Anaphylactic List, Restriction List, Diabetic List, Labels** — calls `builders[type]()` bare; with XLSX undefined (offline / file:// / CDN outage) it throws an uncaught ReferenceError. There is **no `window.onerror` handler** in the file, so the click does nothing visible.
- evidence: `<script src="https://cdnjs.cloudflare.com/...xlsx.bundle.min.js">` (no integrity attr; grep `integrity=` → 0 hits); `const wb = builders[type]();` at :20443 with no `typeof XLSX` guard; `new Chart(ctx,...)` ×6 unguarded.
- consequence: On an offline tablet or during a CDN outage, staff click "Anaphylactic List" and nothing happens — no error, no degradation notice. This contradicts the repo rule "Graceful degradation from `file://`" and is the lens-5 class (silent failure on a load-bearing output) on the highest-stakes documents. `downloadAllFiles` is caught but its toast says "try downloading files individually" — which will also fail, misdirecting staff.
- direction: The existing guard pattern at :11163 (`typeof XLSX === 'undefined'` → visible button state) is the in-house seam — apply it at `downloadSpecialFile`/`downloadPlatingSheet`/`exportTestPlating`, or centrally at the export-button render. SRI pinning is a one-line-per-tag hardening consistent with the 2026-06-18 security pass.

### D-1-2 · `buildPlatingXlsx` silently omits failed days from the downloaded workbook
- class: footgun
- severity: P1
- status: NEW
- confidence: verified
- where: index.html:20082-20101 (`buildPlatingXlsx`), :20105-20108 (`downloadPlatingSheet`)
- what: The per-day try/catch ("One bad day shouldn't kill the whole week download") pushes errors to console only. The workbook is returned and downloaded with the failed day's sheet **missing**; no toast, no UI notice. If ALL days fail, `wb` is null and the download click silently does nothing.
- evidence: `catch (e) { errors.push(...); console.error(...) }` then `if (errors.length) { console.warn(...) }` — no `showToast`, no visible marker; `downloadPlatingSheet` = `if (wb) XLSX.writeFile(wb, filename);` (no else branch).
- consequence: A kitchen prints/works from a weekly plating workbook believing it is complete; a day with a data defect (the exact day most needing attention) is simply absent. Degrade-visibly violation on a printed food-safety document — worse than failing the whole download.
- direction: `showToast(...,'error')` already exists and is used by the sibling `downloadAllFiles` catch (:20413) — surface the skipped-day list there; or refuse the download and name the failing day (Gate-9's "block + name the defect" is the in-house precedent).

### D-1-3 · KNOWN_VS_SHOWN register re-verified at HEAD — C1/NEW-1/H2/H3/M4 all still open; M5 half-fixed
- class: footgun (C1 = food-safety trust surface)
- severity: P1 (C1); P2 (rest)
- status: KNOWN → KNOWN_VS_SHOWN_FINDINGS.md (2026-06-21); fresh line numbers + deltas below
- confidence: verified
- where: see per-item lines
- what: The repo's own known≠shown register is ~8 weeks old and largely unaddressed. Fresh HEAD verification:
  - **C1 (CRITICAL, still open)** — the anaph banner raw-tag re-scrape survives at all four sinks: xlsx banner `:19921-19922`, xlsx section header `:19940`, HTML banner `:20704-20705`, HTML header `:20720`. All use `r.tags.filter(t=>t.startsWith('No '))`, ignoring `anaphTags` scoping and **dropping `'GF'`** — which IS an enforced trigger (`ANAPH_TAG_TO_MEAL_FLAG :8840-8846` maps `'GF':'hasGluten'`). A gluten-only anaphylactic still renders "ANAPHYLACTIC ALERT — Rooms X ()" with the lethal allergen omitted. The faithful enforcer (`getAnaphConflictRooms :9121`, `getAnaphAlertInfo :9147`) remains unused by the banner text.
  - **NEW-1 (still open)** — `buildRegistrySummaryJSON :11678` still tallies `diabetic:sc['Diabetic']||0` etc. from frozen `r.section`; publishes 0s for merged sections. Validator `:11969` still checks only that `headcounts{}` exists.
  - **H2 (still open)** — plating dashboard `mealCell` still tallies `short[r.section]` `:20576` beside a live anaph count.
  - **H3 (still open)** — registry Section pill still renders frozen `r.section`.
  - **M4 (still open)** — anaph emergency xlsx still defaults `r.section || 'Halal'` `:20211` (siblings default 'Regular').
  - **M5/NEW-2 (HALF-fixed)** — `buildMenuJSON :11649` and `buildRoutingByMealJSON :11767` now stamp `DOOR_SCHEMA_VERSIONS.*` (confirmed-healthy); but `buildStateJSON :11656` and `buildRegistrySummaryJSON :11677` still hand-type `version:30`. The fix stopped at the two artifacts EXPO gates on; the residue keeps the drift class alive for the other two.
  - **Minor (still present)** — duplicate `function showChangeLog` (first def dead, second XSS-hardened wins).
- consequence: The single red banner the kitchen acts on can still name wrong allergens and omit the real one — open for 8 weeks with the register in-repo. The half-fixed M5 shows the register is being worked piecemeal without a closure sweep.
- direction: C1's fix is fully specified in the register (reuse `getAnaphAlertInfo`'s per-room `allergen`); the fork it names for Jason (banner lists trigger vs full profile) is still un-ruled — that fork, not engineering, appears to be the blocker. Worth surfacing in the sweep report as "oldest open food-safety item".

### D-1-4 · `_ghHydrated` is a ceremony, not a boundary — it flips true even when every hydration fetch failed
- class: footgun
- severity: P2
- status: NEW (adjacent to KNOWN D1/E2 family, but a distinct mechanism)
- confidence: verified
- where: index.html:19205-19209 (arming), :12145-12148 (the guard it feeds)
- what: `_ghPushFileNow` refuses to publish "not hydrated from cloud yet" — but the flag arms via `Promise.allSettled([...fetch...])` where each fetch already has `.catch(()=>null)`, so it resolves **immediately on network failure**, plus an unconditional 5s `setTimeout`. A device whose boot pulls all failed (captive portal, DNS, offline) is declared "hydrated" within milliseconds and may then publish its stale local state over the cloud on the next user edit. The remaining rails are size-based only (`empty_clobber`, `size_regression`) — a same-size stale registry/menu sails through.
- evidence: `Promise.allSettled([...]).then(()=>{ _ghHydrated = true; })` + `setTimeout(()=>{ _ghHydrated = true; }, 5000);` — no success-count check; guard comment reads "not hydrated from cloud yet".
- consequence: Multi-device regression vector (Jason works home+work): an un-hydrated device's edit pushes 10 files of older state; door_state PULL has a provenance freshness guard but PUSH has none. Mitigated by the visible red sync bar when the boot pull fails, and by the overlay day-union merge — but registry/door_state pushes are whole-file.
- direction: The pull side already owns the pattern — `pullStateFromGitHub`'s `_meta.exported` vs local provenance compare (:10190-10199). Arm `_ghHydrated` only on ≥1 successful settle, or gate the push on the same provenance compare (which is also silent-drift plan E2's shape, DOOR-side).

### D-1-5 · Publish trigger census: 20 `publishAndSync` sites + 7 `sidePublish` sites, zero debounce/coalescing; SHA prefetches carry no cache-buster
- class: footgun
- severity: P1 (KNOWN — this is fresh quantified evidence for silent-drift slice D1)
- status: KNOWN → HOUSE_SILENT_DRIFT_ACTION_PLAN §10 D1; post-defuse handoff §1 (Aug-15 409 incident)
- confidence: verified
- where: index.html — publishAndSync callers: :7938,:8703,:9881,:9981,:10335,:10418,:10558,:10570,:10593,:10843,:11031,:11142,:11601,:11609,:14863,:15115,:16923,:19542,:19631,:19686; sidePublish callers: :9276,:10141,:14042,:17101,:17589,:17601,:19234; SHA fetches :12150,:12178
- what: Every registry edit, compliance field change, anaph room add/remove, menu edit, swap, and intake save fires a full 10-file publish wave with no debounce — 20 trigger sites (`publishAndSync` has no coalescing: it calls `publishToGitHub(false)` directly). Two adjacent edits = overlapping waves = the Aug-15 409s. Compounding: both SHA prefetches in `_ghPushFileNow` (`:12150` initial, `:12178` 409-retry) use plain `fetch(url,{headers})` with **no cache-buster and no `cache:'no-store'`**, while the sibling pull path (`pullStateFromGitHub :10176`) does bust (`'?_='+Date.now()`) — so the 409-retry can re-grab the same stale cached SHA and fail loudly, exactly as the incident log describes.
- evidence: grep counts above; `const ex=await fetch(url,{headers})` vs `rawUrl = '...door_state.json?_=' + Date.now()`.
- consequence: KNOWN (D1). Fresh angle: even before the atomic-push port, the retry could be made effective with `cache:'no-store'` on the two SHA fetches — a two-token change consistent with the repo's own pull-side idiom.
- direction: D1 (port `pushFilesToGitHubAtomic`) is the ruled fix; the cache-buster gap is a stopgap the D1 implementer should know about.

### D-1-6 · Cloud-merge conflict polarity is inconsistent across sibling boot merges (batched P3s)
- class: kludge
- severity: P3
- status: NEW
- confidence: verified
- where: index.html:19210 (meal_swaps), :19180-19183 (overlay), :19246-19250 (custom_tag_rules), :19257+ (learned_nr), :19214-19218 (recent_log)
- what: Batched consistency nits in the boot cloud-merge block. (a) Overlay merge documents+implements **local-wins** per day (`doorMergeMenuOverlayWithCloud` "Local entries always win"); meal_swaps does `Object.assign({}, _local, d)` — **cloud-wins** per key; learned_nr also cloud-wins; custom_tag_rules local-wins by pattern. No comment justifies the polarity choice per store. (b) recent_log dedupe key is `e.room+e.date+e.desc` where `date` is a year-less label ("Aug 15") — cross-year collisions silently drop legitimate entries (log capped at 200 so drift is bounded). (c) The whole `bootRemoteState` block ends `} catch(e) {}` (:19252) — any sync throw in the setup (not the promises) is silent.
- consequence: A device that made the most recent swap but failed to push can have it reverted by an older cloud copy at next boot, silently — low frequency, informational-to-operational stores. Maintainer cost: each merge must be reasoned about from scratch.
- direction: One conflict-polarity convention (the overlay's documented local-wins + day-union is the in-house seam) or a one-line comment per store naming why cloud wins there.

### D-1-7 · Baked CODEX fallback predates the vegan-stream re-tagging — stream-keyed safety lookups silently inert on the fallback tier
- class: data
- severity: P2
- status: NEW (distinct from KNOWN DOOR #63, which is the matcher's narrowness on live data)
- confidence: verified
- where: index.html:742 (`DOOR_RECIPE_DATA_FALLBACK`), :16395-16397 (`getVegAltAllergenStr` vegan-stream filter), :782 (badge)
- what: The baked fallback carries **131 records, all `stream:"regular"`**; the live CODEX feed carries **240 records incl. 73 `stream:"vegan"`** (measured against the sibling repo's committed `DOOR_RECIPE_DATA.json`). Every stream-keyed lookup — `getVegAltAllergenStr` (filters `r.stream==='vegan'`) and the slot-search stream filters (`recipeMatchesSlotDef`) — finds an **empty candidate set** on the fallback tier. The veg-alt advisory line then always falls back to the broad main-meal allergens (safe direction, but different output for the same menu depending on network state), and vegan slots cannot recipe-link offline.
- evidence: `grep '"stream": "vegan"'` on line 742 → 0 of 131; live feed Counter = {regular:167, vegan:73}.
- consequence: A first-boot-offline device (no `door_recipe_data_cache` yet) prints veg-alt advisories that differ from every online device's output, with only the generic "CODEX Offline" badge as a hint. The fallback is ~2 months stale (predates CODEX's July-18 plant-forward re-tagging) and structurally missing the dimension a food-safety-adjacent lookup keys on.
- direction: The fallback-refresh is a data-only paste of the live feed (the file even documents it as a snapshot). A door-smoke gate asserting "fallback contains ≥1 vegan-stream record" would pin the class (the in-house authored-to-fail idiom).

### D-1-8 · Import fuzzy pass writes routing tags with no provenance marker; Lev≤2 admits real-word collisions
- class: footgun
- severity: P2
- status: NEW (sibling of KNOWN DOOR #63 per brief's "hunt siblings" instruction)
- confidence: verified
- where: index.html:17843-17854 (`IMPORT_FUZZY_VOCAB`), :18001-18016 (fuzzy pass), :17828 (`importLevenshtein`)
- what: After exact rules, leftover tokens fuzzy-match the vocab at Lev ≤ 2 (length-diff ≤ 3) and set `tags[best]=true` **indistinguishably from an exact match** — no `_fuzzy` provenance, no review-queue entry (only unmatched tokens surface to the NR review flow). Real-word collisions exist at distance ≤2: `"salad"→'halal'` (dist 2), `"no beer"→'noBeef'` (dist 1) — both plausible tokens in messy restriction cells, both changing meal routing (over-restrictive direction).
- evidence: `if (best && bestDist <= 2) { tags[best] = true; } else { unmatched.push(tok); }` — matched tokens never reach `unmatched`.
- consequence: A resident silently acquires a wrong routing tag from a typo-distance real word; the import diff screen shows resulting tags (mitigation), but nothing marks which tags were guessed, so a reviewer has no reason to scrutinize them. All identified collisions are over-restriction (safe direction for allergy, wrong for service).
- direction: The NR/learned-review flow (`concUnresolvedNR` → review queue) is the existing seam for uncertain parses — route fuzzy hits (dist ≥1) through it, or stamp them into `unmatched`-style visibility like the duplicate-room detector (:18355) does.

