# Findings — Slice E-1 · EXPO shell / boot / storage / matching / decomposition (L1–12,074) + whole-file battery + boot timeline

- Date: 2026-08-15 (relaunch after 18:20 UTC cap outage)
- Repo: `/home/user/conc-kitchen-expo` @ HEAD **2686de6** "Merge pull request #232 from kennedyjasondavid-eng/claude/expo-version-bump-v9.49.36"
- File: `index.html` 40,463 lines
- Coverage statement: IN PROGRESS — will be finalized at end of run.
  - Attentive: L1–12,074 (CSS sampled per brief; mega single-line data tables script-checked, not read).
  - Whole-file (grep/script scale): grep battery, inline-handler set-diff, boot-timeline artifact, empty-catch triage.

## Grep-battery results

(appended as measured)

## Findings

(appended as confirmed)

## Boot-timeline artifact

(required section — appended after trace)

## Confirmed-healthy notes

(appended)

## Limitations

(appended)

---
## Grep-battery results (whole file, 40,463 lines, HEAD 2686de6)

| probe | count | notes |
|---|---|---|
| empty catches `catch(..){}` | **132** (of 258 total = 51%) | triage of boot/storage-path ones below (finding E1-x) |
| `innerHTML =` | 111 | escape-helper unification (J1) covered the named-helper family; not re-audited sink-by-sink here (M-pass precedent) |
| `insertAdjacentHTML` / `outerHTML=` | 0 / 0 | — |
| `document.write` | 8 | all in print-window flows (`:21510`, order-sheet `:39044-39050`) — writes to a `window.open()` child doc, not the live DOM; acceptable pattern |
| `localStorage` get/set/remove | 170 | 26 with constructed keys — all use named helper/key-builder fns (`_orderSheetBucketKey`, `colKey`, store maps); no ad-hoc string-concat keys found outside those |
| bare `JSON.parse` (no same-line try) | 84 of 118 | most are inside multi-line try blocks; boot-path ones traced in boot timeline |
| inline `on*="` handlers | 396 | set-diff vs defined functions: see finding + artifact below |
| `setTimeout(` | 30 | sequencing-flavored: `initHoverPreview` 1000ms `:23752`, boot snapshot drain 1000ms `:39775`, `.mob` activate 500ms `:39882/84`, add-search focus 200ms `:19308` — all non-load-bearing UI timing except the boot drain (see boot timeline B-13) |
| `new Date('string')` | 2 | **both are the STALE May-10 anchor** — finding E1-1 |
| duplicate `function <name>` decls | `attempt` ×2, `render` ×2 | verified NOT true duplicates: both `attempt`s nested in different parents (`:22425` in `pushFileToGitHub`, `:22501` in `pushFilesToGitHubAtomic`); `render` `:18898` top-level vs `:40381` nested in env-badge IIFE. Healthy. |
| version order-compares (`<`/`>` near version ids) | 0 hits | version compares are equality-only — conforms to the D7 contract. Healthy. |
| `localeCompare` | 10 | all are name-sort comparators, none on version strings. Healthy. |
| self-confessed markers | legacy 276 · deprecated 0 · "for now" 1 · TODO 2 · FIXME 0 · workaround 0 · shim 1 | "legacy" density is mostly the intentional Ledger migration-bridge vocabulary (`_resolveItemLegacy`, legacy stores), not drive-by confessions |

---
## Findings

### E-1-1 · Parse-time TDZ crash sibling LIVE in `compileProductionFacts` Path 4 (`_RAW_SEND_ARCHETYPES`)
- class: footgun
- severity: P1
- status: NEW (sibling of the FIXED v9.49.28 `scheduleMode` TDZ; that fix + both its gates do NOT cover this)
- confidence: verified
- where: `conc-kitchen-expo/index.html:3985` (read) vs `:10982` (declaration); parse-time call chain `:9693` merge → `_materializeCustomRecipe:4066` → `compileProductionFacts:3855`
- what: The parse-time custom-recipe merge (`Object.entries(_customRecipes).forEach` @9693, unguarded — no try/catch) calls `compileProductionFacts` for any `pp_custom_recipes` entry carrying `productionFacts`. Its **Path 4** branch ("raw-to-service-cook": steps = [advance prep cold, sameDay cook at serve site]) evaluates `_RAW_SEND_ARCHETYPES.has(seed.archetype)` at `:3985` — but `const _RAW_SEND_ARCHETYPES` is declared at `:10982`, ~1,300 lines below the merge. During parse it is in its temporal dead zone; entering Path 4 throws `ReferenceError`, aborting the whole script → **blank board**, the identical v9.49.28 symptom.
- evidence: `:3985 const seedArch = _RAW_SEND_ARCHETYPES.has(seed.archetype) ? seed.archetype : '';` inside the `steps[0].action==='prep' && steps[1].action==='cook'` branch; `:10982 const _RAW_SEND_ARCHETYPES = new Set([...])`; merge block `:9693-9697` has no try/catch. **Both existing gates miss it:** `boot_schedulemode_order_gate` pins only the `scheduleMode` declaration; `tests/fixtures/populated_boot.json` carries a single-step sameDay cook (verified: `steps:[{action:'cook',timing:{mode:'sameDay'}}]`) which never reaches Path 4 — the gate is green *because* the fixture takes a different path (INSIGHTS lens 4: gate green because the feature is inert).
- consequence: The New Item Wizard's facts flow exists precisely to describe patterns like "Bloor preps raw → Rex cooks fresh on service day" (the standard-mode raw-send shape: tofu/fish/legs). The first operator who saves such a recipe gets a working session, then a **dead app on every subsequent boot on that device** until localStorage is hand-cleared — the exact class that blanked Jason's board 2026-07-30.
- direction: The v9.49.28 fix pattern is the existing seam: hoist `_RAW_SEND_ARCHETYPES` (and its sibling `_SERVE_SITE_ARCHETYPES` @10985, same block) up beside the routing constants above the merge — or above `compileProductionFacts` itself; add a Path-4 (prep→cook) facts entry to `populated_boot.json` so the boot-smoke gate covers more than one compiler path.

### E-1-2 · Stale hardcoded date anchors: day-sheet labels print May-2026 dates forever
- class: data
- severity: P1
- status: NEW (the same May-10 anchor class was fixed for the resource graph + board headers in UX-audit H1, 2026-07-03 — these two copies were missed)
- confidence: verified
- where: `conc-kitchen-expo/index.html:33631` (`dayDateLabel`) and `:33689` (`getWeekDateRange`); consumed by the Day-Sheet view header `:33518` and its 🖨 Print button (`printDaySheet`, HTML shell `:1867`)
- what: Both helpers compute dates from `new Date('2026-05-10')` ("Wk1 Sunday (re-anchored 2026-05-19)") + `(week-1)*7 + dayIdx` — no cycle projection to today. The canonical anchor is `HUB_CYCLE_START` (Jun 7, `:21756`) with `serviceDateForSlot()` (`:21853`, whose comment calls itself "the ONE" seam). Two defects stack: (a) the labels are frozen to the May–June 2026 window — 14+ months stale today and drifting forever; (b) `new Date('YYYY-MM-DD')` parses as **UTC midnight**, so `.toLocaleDateString('en-CA')` in Toronto renders the **previous day** (May 9, a Saturday, for the "Wk1 Sunday" anchor) — even in its own era every label was off by one.
- evidence: `:33631 const anchor = new Date('2026-05-10');` · `:33691` same; `:33518` \`Week ${currentWeek}${dateRange ? ' · ' + dateRange : ''}\` renders into `#daySheetWeekLabel`; grep `new Date('` whole-file = exactly these 2.
- consequence: A staff member opening or printing the Day Sheet view sees "Week 2 · May 16 – May 22" style headers in August 2026 — a staff-facing print artifact carrying confidently wrong dates (Known≠shown; trust surface). Anyone cross-checking against a calendar will distrust the sheet or, worse, hand it out with wrong dates.
- direction: Repoint both helpers at the existing `HUB_CYCLE_START` + `serviceDateForSlot()`/`_ymdLocal` seam (exactly what H1 did for board headers); `hub_cycle_anchor_gate` already exists as the gate home for a regression assert.
