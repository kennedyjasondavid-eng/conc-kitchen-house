# Fork rulings — HOUSE code sweep §5 (Jason, 2026-08-16)

All nine forks from `HOUSE_CODE_SWEEP_REPORT_2026-08-15.md` §5 were ruled 2026-08-16. Verbatim rulings + the operational reading each was executed under. Status column tracks delivery.

| Fork | Ruling (verbatim) | Operational reading | Status |
|---|---|---|---|
| **F1** (E-2-2, EXPO frozen PULLs) | "Rarely does frozen stuff travel Rex->Bloor, but the functionality to allow it in expo should remain" | The absent PULL rows are **mostly correct** — routine every-cycle frozen transfers are not the operating reality, so the passes stay dormant in standard. **Nothing is deleted**: `generatePullSteps` / `applyRoutingDrivenQty` / `FROZEN_STOCK_DB` remain. The doc drift gets corrected (CLAUDE.md v9.29.3 presents routing-driven qty as live; it is not, in standard). The **rare-case lever** — how a supervisor invokes a frozen Rex→Bloor pull when one genuinely happens — is a small design slice (per-recipe fact vs. profile capability vs. Add-flow affordance), not guessed at here. The `isLANActive()` gate remains the de-facto dormancy switch until that slice replaces it with a named lever. | **Built — EXPO PR #236 (MERGED 2026-08-16)**: gate-site + pipeline comments state dormant-by-ruling/KEEP, CLAUDE.md v9.29.3 carries the dated ⚠ CORRECTION. Lever = open design slice (not built) |
| **F2** (D-1-3/C1, DOOR anaph banner) | "follow lean" | Banner names each room's **trigger allergen**, sourced from the routing enforcer (`getAnaphAlertInfo`) — one source of truth, same code that acts. Full profile stays on registry surfaces. Closes the ~8-week-old C1 register item; includes the D-1-9 authored-to-fail test (a gluten-only anaphylactic must render its allergen). | **Built — DOOR PR (this batch)** |
| **F3** (H-1-5, HUB freshness compare) | "Follow lean" | **Widen** the MEALS contract: EXPO's writer emits veg/halal strings, HUB's `compareBoardMenuToDoor` compares them — writer + reader in one coordinated change set per the contract rule. Until that lands, the interim honest relabel ("Regular mains match DOOR") is acceptable. | **Built — EXPO PR #237 (v9.49.38, `hub_meals_contract_gate`) + HUB PR #35 (`compareBoardMenuToDoor` widened, absent-key tolerance, `schedule_freshness_contract` re-pinned), BOTH MERGED 2026-08-16.** Full veg/halal coverage activates on the next EXPO republish (the first v9.49.38 publish widens the live payload; until then HUB compares the 56 regular-main slots) |
| **F4** (H-1-3, HUB April seed block) | "Follow lean" | **Delete** the seeding IIFE — HUB stops being a second writer to MISE's `concRecipeLibrary`. One-time check first: any seeded record that is real and used gets handed to MISE's governed seed pipeline instead of silently dying. | **Built — HUB PR (this batch)** |
| **F5** (P-1-7, portal PIN) | "Follow lean (lowest priority on this list)" | Reframe honestly at zero cost: "ACCESS"/"PIN required" language dropped, modal retitled "Staff check — <APP>", an in-modal note says plainly it is not a lock. PIN mechanics + lock-icon affordance kept. Real access control remains the Entra/SharePoint roadmap item (IT brief), which this dialog must never impersonate. | **Built — HOUSE PR (this batch)** |
| **F6** (P-1-8, PROOF portal card) | "Not right now - defer" | PROOF stays off the portal; nothing touched. Recorded here so future sweeps/readers see the omission is a **deliberate deferral**, not an oversight. Revisit when Jason chooses. | **Deferred (recorded)** |
| **F7** (H-1-11, HUB "Kitchen" role) | "follow lean" | Kitchen role = **all cook-sites, derived** — membership comes from the sites actually carrying cook work (via the `HUB_HOME_SITES` registry), not a hardcoded `['Bloor','LAN']`. In standard mode that means Rex's 95 COOKs join the Kitchen view. | **Built — HUB PR (this batch)** |
| **F8** (M-2-2, MISE "Publish Now") | "follow lean but that language is opaque" | Make the button honest, don't wire it live — **and say it in plain kitchen words**, not runbook-speak. The button states up front that publishing from the app is turned off and recipe updates go out through Jason's review; Test Connection / Dry Run stay as diagnostics. The hardcoded approval constant stays the boundary. | **Built — MISE PR (this batch)** |
| **F9** (H-1-14, HUB archive pages) | "follow lean" | One-line retirement banner on each archived HTML page + the archived Connect form's token-remove lines neutered, so a stale bookmark can no longer silently wipe the shared `conc_gh_token`. Live code untouched. | **Built — HUB PR (this batch)** |

Sequencing note (resolved): the EXPO-side items (F1 doc fix, F3 writer half) waited for sweep PR conc-kitchen-expo#235 to merge — all repos share the one designated branch, and stacking ruling-driven work onto the reviewed sweep-fix PR would have muddied it. Delivered in order on 2026-08-16: #235 → #236 (F1) → #237 + hub#35 (F3). **All nine rulings are now closed** — seven built and merged, F6 deferred by ruling, F1's rare-case lever left as the one open design slice.
