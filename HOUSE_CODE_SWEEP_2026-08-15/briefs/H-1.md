# Slice H-1 — HUB full app + tests + workflows + archive greps

Read `_COMMON.md` first. Findings → `findings/H-1.md`. Repo: `/home/user/conc-kitchen-hub` (record HEAD sha).

**App:** `CONC_Production_Hub.html`, 6,230 lines — the staff-facing daily board, **used live every shift on phones/tablets/print**. It renders EXPO's published schedule; it never computes. `index.html` is a 1-line redirect.

**Live-daily P0 lens:** this board is what staff cook from TODAY. Priorities: known≠shown (stale shown as fresh, wrong-day shown as today), silent failure on the render path, print fidelity.

**Attentive: the full file**, with named attention:
- **Date/day-boundary math**: `projectDates()`, `getTodayInfo()` (claims LOCAL ISO — verify), `serviceDateForSlot`-equivalents, the `__HUB_NOW__` test seam, 28-day cycle wrap. What renders between midnight and first refresh? (P-1 owns house-wide TZ; you own HUB's correctness specifically.)
- **The loader**: `loadScheduleData` single-flight guard, sidecar version check (opaque-equality contract — verify no order-compare crept in), 3-tier fallback, `_readCache`/`_writeCache`, the freshness-contract block (`compareBoardMenuToDoor`, `_refreshDoorMenuForFreshness` throttle, TDZ-sensitive state placement — a documented boot-order constraint; verify it still holds).
- **Notes/overrides**: the `HUB NOTES CONFLICT CONTRACT` (H1, shipped) — verify the contract's edges (what happens on double-tab same-device? on PUT 409 mid-retry?). H2/H3/H4 are KNOWN open — hunt *adjacent* defects only.
- **`@media print` in full** — drivers/kitchen print this; can a filter/role state make the print silently incomplete?
- **Orders/costing path** + `rcpUrlWithFallback` / `_componentPortions` (fuzzy 12-char rule; conflicting-hits→no-portions rule — verify still true).
- **The 5 inline site lists** (recon: `KNOWN_SITES` @~3702 incl. GCNC, local `SITES` fallbacks @~5567/@~5683, `SITE_ORDER` @~5844, `SITES=['Bloor','LAN']` @~5997) — for EACH: what live behavior does it drive, and where does its divergence from `HUB_HOME_SITES` have real effect (e.g. is @5997 silently excluding Rex from something)? X-C owns the cross-app vocab table; you own HUB-internal consequences.
- 17 empty catches; 51 innerHTML sinks (schedule/notes text — notes are multi-user-authored via overrides: untrusted at the sink; verify escaping).
- CLAUDE.md doc-drift: the "live payload has no `_schemaVersion`" note (dated 07-27) vs HEAD `hub_schedule.json` carrying `_schemaVersion: 2` — verify + report as doc-drift with the H4 one-directional-audit implication.

**Tests + CI:** the 4 gates (`home_site_slice`, `stage_nocard_contract`, `schedule_freshness_contract`, `notes_conflict_contract`) — do they cover the loader fallback tiers and date projection, or only the contract blocks? `deploy.yml`: it copies `recipe_production.json` into the Pages site — who reads HUB's copy vs CODEX's original (second-copy lens; is it even fetched from HUB's origin path by anything)?

**`archive/` (6K lines, deprecated): grep-only** — (a) does the live file reference anything in archive/? (b) do archived files write/read localStorage keys the live app also uses (a stale-open archived page = a surprise writer)?
